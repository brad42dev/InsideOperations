# Review (deep)

**Generated**: 2026-05-16T08:32:27+00:00
**Log**: `/home/io/io-dev/io/.claude/logs/2026-05-16_general_025143.md`
**Session**: 
**Depth**: deep

---

## Summary

The work unit delivered two parallel workstreams: (1) hook infrastructure fixes for recursive review triggers, and (2) versioning UI polish including a URL navigation bug fix, z-index fix, and chart version history entry point. All three areas appear correctly implemented in the provided file contents. The primary hook concern — recursive action triggers from `claude -p` sub-sessions finding embedded tags in log content — is addressed with belt-and-suspenders: a file-based atomic lock in `stop.sh` plus known-tag sanitization in `extract-turn.sh`. The versioning changes are structurally sound. One genuine gap remains open from the session: `#ASSISTANT` block capture timing was identified but no code fix was made, and the session summary acknowledges uncertainty about whether it's already working.

## Concerns

1. **`#ASSISTANT` capture timing — open, unresolved.** The work unit summary explicitly says "a code fix was not confirmed in this log" for the `#ASSISTANT` block issue. The session identified a timing concern (Stop fires before transcript JSONL is complete, so assistant text entries may not yet exist when `extract-turn.sh` runs), noted it "may already be working," but left no definitive fix in place. The `extract-turn.sh` code *does* handle `type == "text"` under the assistant branch, but whether it works consistently at Stop-hook timing is unverified. This is a real gap if review fidelity matters.

2. **`user-prompt-submit.sh` stale-lock cleanup uses wall-clock age, not process liveness.** The lock cleanup at 900 seconds (15 min) is a TTL heuristic. If `wrapup` spawns three sequential `claude -p` calls at 180s each, the total is 540s — under the threshold. But if the timeout for any call is extended or the calls are retried, the lock could be held legitimately past 900s and get prematurely cleaned, re-enabling the recursion the lock was designed to prevent. `user-prompt-submit.sh:40–46` (approximate): the cleanup has no awareness of whether the lock-holding process is still alive.

3. **`extract-turn.sh` early-exit path omits `#TURN_END`.** When `$TRANSCRIPT_SLICE` is empty (nothing new since prompt submission), the script writes a `#PROMPT` block and exits. No `#TURN_END` marker is appended. This is acknowledged as pre-existing in the session, but it means work-unit logs can have mismatched structure (prompts without matching turn-end markers) if timing causes early exits. The deep review noted this as not fixed.

4. **`stop.sh` lock acquired before second dispatch case — `*` (none) path never reaches lock release.** For `TAG_TYPE=none` (plain prompts), the first `case` statement does not match, so no lock is acquired. The second `case` statement also falls to `*` and does nothing. This is correct. However, if a future tag type is added to the first case (lock acquisition) but omitted from the second case (lock release subshell), the lock would be held until the 15-minute stale cleanup. This is a structural fragility: the lock-acquire case and the lock-release locations are non-adjacent and must be kept in sync manually.

5. **`designer/index.tsx` save-handler `navigate()` call cannot be verified from provided file contents.** The file is truncated at the `lockHeldRef = useRef(false)` declaration, well before the save handler where `navigate(\`/designer/graphics/${id}/edit\`, { replace: true })` would appear. The smoke test confirmed the URL updates after first save, and typecheck was clean — but the actual implementation cannot be confirmed from the provided material.

## Verification Notes

The recursive-review flood visible in the work unit log (6 nested copies of the shallow-review prompt between 05:02:43 and 05:03:19) is documented history of the bug being fixed, not a current defect. The fix was implemented correctly after that point.

The `sanitize_tags` regex in `extract-turn.sh` uses `[a-z0-9-]+` (one or more) for the docfresh slug pattern, matching the tag detector in `lib-common.sh` — the `*`→`+` fix from the deep review was confirmed applied.

The `VersionRecoveryDialog` z-index values (backdrop 3100, panel 3101) correctly exceed the AppShell topbar at zIndex 3000, resolving the close-button intercept bug observed during smoke testing.
