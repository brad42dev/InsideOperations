export default function AlertComposer() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Send Alert</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Compose and send a human-initiated emergency alert — Phase 14
      </p>
    </div>
  )
}
