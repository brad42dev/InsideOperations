export default function ActiveAlerts() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Active Alerts</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Currently active human-initiated alerts — Phase 14
      </p>
    </div>
  )
}
