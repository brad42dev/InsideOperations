export default function AlertGroups() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Alert Groups</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Recipient groups for emergency alerts — Phase 14
      </p>
    </div>
  )
}
