export default function Snapshots() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Change Snapshots</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Configuration change snapshots and diff export — Phase 9
      </p>
    </div>
  )
}
