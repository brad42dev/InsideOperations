export default function Badges() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Badges</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Badge reader integration and access configuration — Phase 15
      </p>
    </div>
  )
}
