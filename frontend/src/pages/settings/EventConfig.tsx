export default function EventConfig() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Events</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        ISA-18.2 alarm and event configuration — Phase 9
      </p>
    </div>
  )
}
