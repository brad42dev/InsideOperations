export default function Display() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Display Preferences</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Personal display and density preferences — Phase 4
      </p>
    </div>
  )
}
