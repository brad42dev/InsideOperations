export default function ShiftSchedule() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Shift Schedule</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Shift rotation schedule and calendar — Phase 15
      </p>
    </div>
  )
}
