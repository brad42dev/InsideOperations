export default function MusterPointConfig() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Muster Points</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Emergency muster point configuration — Phase 15
      </p>
    </div>
  )
}
