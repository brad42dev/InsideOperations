export default function ShiftScheduleEditor() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Edit Shift Schedule</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Configure shift rotations and crew assignments — Phase 15
      </p>
    </div>
  )
}
