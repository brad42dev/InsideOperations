export default function RoundHistory() {
  return (
    <div style={{ padding: 'var(--io-space-6)' }}>
      <h2 style={{ color: 'var(--io-text-primary)' }}>Round History</h2>
      <p style={{ color: 'var(--io-text-secondary)' }}>
        Completed and cancelled inspection round history — Phase 13
      </p>
    </div>
  )
}
