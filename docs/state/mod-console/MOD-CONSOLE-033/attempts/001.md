---
attempt: 1
task_id: MOD-CONSOLE-033
started: 2026-03-26T10:00:00Z
closed: 2026-03-26T10:15:00Z
result: SUCCESS
---

## What Was Attempted

Fixed the wrong endpoint in `graphicsApi.list()`. Changed the base URL from `/api/v1/design-objects` (shape library endpoint, ignores scope) to `/api/graphics` (graphics list endpoint, filters by type='graphic'). Renamed the `scope` param to `module` to match the backend handler's query param name. Updated all three call sites (ConsolePalette.tsx, PaneWrapper.tsx, process/index.tsx) to pass `module` instead of `scope`.

## Files Modified

- `frontend/src/api/graphics.ts`: changed `list()` URL from `/api/v1/design-objects` to `/api/graphics`, renamed param `scope` to `module` in the type signature and call
- `frontend/src/pages/console/ConsolePalette.tsx` line 1575: `{ scope: 'console' }` → `{ module: 'console' }`
- `frontend/src/pages/console/PaneWrapper.tsx` line 164: `{ scope: 'console' }` → `{ module: 'console' }`
- `frontend/src/pages/process/index.tsx` line 585: `{ scope: 'process' }` → `{ module: 'process' }`

## Changed Files

before_state: 41c9d7ee29c42595921e2c52a2e0283a8ab1d706
changed_files:
  - frontend/src/api/graphics.ts
  - frontend/src/pages/console/ConsolePalette.tsx
  - frontend/src/pages/console/PaneWrapper.tsx
  - frontend/src/pages/process/index.tsx
  - frontend/tsconfig.tsbuildinfo

changed_functions: (for human review only — not used in automated cycle detection)
  - graphicsApi.list (signature change: scope -> module, URL change)
  - ConsolePalette queryFn (call site update)
  - PaneWrapper queryFn (call site update)
  - process index queryFn (call site update)

## Verification

command: npx tsc --noEmit
result: PASS
output: exit 0, 0 errors

command: pnpm test
result: PASS (pre-existing failures only)
output: 2 failed tests in permissions.test.ts (reports:generate, reports:schedule_manage — pre-existing, unrelated to this task); 477 passed

command: pnpm build
result: PASS
output: BUILD_EXIT:0

command: git diff HEAD | grep TODO/FIXME/stub
result: PASS
output: clean

command: npx madge --circular src/
result: PASS
output: No circular dependency found!

## Checklist Results

- ✅ Console palette graphics section shows only type='graphic' records tagged module='console' — /api/graphics?module=console now used
- ✅ Process graphics picker shows only type='graphic' records tagged module='process' — /api/graphics?module=process now used
- ✅ Shape library (Designer) unaffected — graphicsApi.list() is not used by Designer palette; Designer uses listStencils() and shape endpoints which remain on /api/v1/design-objects
- ✅ TypeScript builds clean (0 new errors)
- ✅ Production build passes (BUILD_EXIT:0)
- ✅ No TODO/FIXME stubs introduced
- ✅ No circular imports
- ✅ Scope check passed — all modified files are in-task scope

## Cycle Check

NO COLLISION — no prior attempts exist for this task.

## Why This Attempt Succeeded

The fix was a targeted 4-file change: one API function signature + URL correction, three call-site param renames. TypeScript caught no type errors. The `module` param aligns with the backend `list_graphics` handler. Designer is unaffected because it uses `listStencils()` and shape-specific endpoints, not `graphicsApi.list()`.

## Notes for Next Attempt

N/A
