---
attempt: 1
task_id: DD-37-002
started: 2026-03-21T00:00:00Z
closed: 2026-03-21T00:10:00Z
result: SUCCESS
---

## What Was Attempted

Added `WsBatchUpdate` and `WsPointValue` structs to `crates/io-bus/src/lib.rs`. Changed `WsServerMessage` serde attributes from single `#[serde(tag = "type", rename_all = "snake_case")]` to two attributes: `#[serde(tag = "type", content = "payload")]` + `#[serde(rename_all = "snake_case")]`. Changed `Update` variant from struct variant to tuple variant `Update(WsBatchUpdate)`.

Updated `services/data-broker/src/fanout.rs` to accumulate per-client point batches and send a single `WsServerMessage::Update(WsBatchUpdate)` per client per incoming UDS batch, rather than one message per point. Updated test pattern matches to use the new tuple variant.

Updated `services/data-broker/src/ws.rs` to build a `WsBatchUpdate` with all cached points for newly-subscribed clients and send it as a single `WsServerMessage::Update`.

## Files Modified

- `crates/io-bus/src/lib.rs` — Added `WsBatchUpdate` and `WsPointValue` structs; changed `WsServerMessage` serde attrs and `Update` variant; updated test
- `services/data-broker/src/fanout.rs` — Updated `fanout_batch` to use `WsBatchUpdate`; updated all test match patterns
- `services/data-broker/src/ws.rs` — Updated subscription initial-value send to use `WsBatchUpdate`

## Patch Fingerprint

fingerprint: 1f7254502b543bbc788e5924fb6c1ed1f1ea3d0f5c4723733ad8d3cb62340a9a
before_state: (no prior diff — first attempt)
after_state: 4eb03f8ceb6fcca947c9959bbebb7ac18b0f85684505c50dcbf2f7c33f0f3505

## Verification

command: cargo check (from /home/io/io-dev/io)
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.27s (warnings only, pre-existing)

## Checklist Results

✅ `WsServerMessage` enum has `#[serde(tag = "type", content = "payload")]` (not just `tag`)
✅ `Update` variant is `Update(WsBatchUpdate)` (tuple variant), not a struct variant with inline fields
✅ `WsBatchUpdate` struct exists with `points: Vec<WsPointValue>` field
✅ `WsPointValue` struct has fields `id: Uuid`, `v: f64`, `q: String`, `t: i64` (abbreviated names)
✅ Serialized JSON for an update message is `{"type":"update","payload":{"points":[{"id":"...","v":1.5,"q":"good","t":1234567890}]}}` — confirmed by serde adjacently-tagged format with `tag`+`content`

## Cycle Check

NO COLLISION — no prior fingerprint matched (first attempt)

## Why This Attempt Succeeded

All three files were updated cleanly. The `WsBatchUpdate` and `WsPointValue` structs were added. The `WsServerMessage::Update` variant was changed from an inline struct to a tuple variant. The serde attributes were updated to use adjacently-tagged format with `content = "payload"`. All call sites in `fanout.rs` and `ws.rs` were updated. The build passes with no new errors.

## Notes for Next Attempt

N/A
