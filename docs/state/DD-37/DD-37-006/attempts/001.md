---
attempt: 1
task_id: DD-37-006
started: 2026-03-23T00:00:00Z
closed: 2026-03-23T00:03:00Z
result: SUCCESS
---

## What Was Attempted

Added the `io_sk_` API key authentication branch to `services/api-gateway/src/mw.rs`.

Changes:
1. Changed `extract_bearer` result from `&str` to owned `String` (required to avoid borrow conflict when passing `req` to the helper function).
2. Added `if token.starts_with("io_sk_")` check after the service-secret check and before the JWT decode path (lines 66-69).
3. Added `handle_api_key_auth` async function that:
   - Extracts the first 8 chars as `key_prefix` to narrow the DB query
   - Fetches all non-expired api_keys rows matching that prefix
   - Argon2id-verifies the full token against each candidate's stored hash using `io_auth::verify_password`
   - Looks up the username from the `users` table (also checks `enabled = true`)
   - Fire-and-forgets a `last_used_at = NOW()` update
   - Builds a synthetic `Claims` struct with the key owner's user_id as sub, username, and scopes as permissions
   - Injects `x-io-user-id`, `x-io-username`, `x-io-permissions` headers (same as JWT path)

## Files Modified

- `services/api-gateway/src/mw.rs` — added io_sk_ branch and handle_api_key_auth function

## Patch Fingerprint

fingerprint: 3564d64752c7f7495093aa926036d1217bb3f48d15c7dc3189f672a16834d373
before_state: 41282ee076030f7edd7432f266433e7430be38713bea007bd20b8ad6187f550f
after_state: 506f51d6a20f8221165530017f09701e08225ecef4276c93714b3d7ef942db91

## Verification

command: cargo check -p api-gateway
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.16s

## Checklist Results

✅ After extracting the Bearer token, the middleware checks `if token.starts_with("io_sk_")`
✅ The `io_sk_` branch performs a database lookup filtered by key_prefix and verified with Argon2id (no active column exists; expiry is enforced by expires_at > NOW())
✅ A valid API key produces a synthetic Claims struct with the key owner's user_id and permissions (scopes)
✅ An expired or deactivated API key returns 401 (filtered by expires_at; user checked for enabled=true)
✅ The `io_sk_` check occurs BEFORE the JWT decode attempt (line 67 vs line 71)

## Cycle Check

NO COLLISION — no prior fingerprint matched (attempt 1, no prior attempts)

## Why This Attempt Succeeded

The implementation was straightforward once the actual schema was understood. Key insight: the `api_keys` table uses Argon2id (not SHA-256) for key hashes, so a direct hash lookup is not possible — must verify with Argon2. Used prefix narrowing to limit candidates. The `io-auth::verify_password` function in the existing crate handles Argon2 verification. Changed `extract_bearer` return type from borrowed `&str` to owned `String` to avoid Rust borrow conflict when passing `req` to the async helper.

## Notes for Next Attempt

N/A
