---
attempt: 1
task_id: DD-22-004
started: 2026-03-21T10:00:00Z
closed: 2026-03-21T10:05:00Z
result: SUCCESS
---

## What Was Attempted

Commented out the active HSTS header line in `/home/io/io-dev/io/nginx/io.nginx.conf`.

The original line 40 was:
```
add_header Strict-Transport-Security  "max-age=31536000; includeSubDomains; preload" always;
```

This was replaced with:
```
# HSTS: disabled by default. Enable via Settings > Certificates after installing a trusted cert.
# add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;
```

The setup-nginx.sh script was also reviewed and contains no direct HSTS line — it only copies the nginx.conf from the repo, so no change was needed there.

## Files Modified

- `nginx/io.nginx.conf` — Commented out active HSTS header; corrected max-age to 63072000; removed preload flag; added explanatory comment pointing to Settings toggle

## Patch Fingerprint

fingerprint: 309446f26ad06ad8ab8b0d8032c63531905170d25a4adbe4481f3ea1f37093ec
before_state: 6cdbb2dd1be298f38834aa2b81640e4461aafd265dea45ddf461ff80626897d0
after_state: bd5dad80bfe5504cf622919223b3c30165a1b851a3fc79495fc1d9fbf26d98eb

## Verification

command: grep "Strict-Transport-Security" /home/io/io-dev/io/nginx/io.nginx.conf
result: PASS
output: `    # add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;`

## Checklist Results

✅ No active (uncommented) `add_header Strict-Transport-Security` line in the default config
✅ Line is commented out with a comment indicating it is managed by the Settings toggle
✅ The spec-documented max-age when enabled is `63072000` (2 years), not `31536000` (1 year)
✅ `preload` flag is absent — spec says preload is explicitly out of scope

## Cycle Check

NO COLLISION — no prior fingerprint matched (first attempt)

## Why This Attempt Succeeded

The fix was straightforward: a single line edit to nginx/io.nginx.conf. The active HSTS header with wrong max-age (31536000) and forbidden preload flag was replaced with a commented-out version using the spec-correct max-age (63072000) and no preload flag. A descriptive comment points admins to the Settings > Certificates toggle for enabling HSTS after a trusted cert is installed.

## Notes for Next Attempt

N/A
