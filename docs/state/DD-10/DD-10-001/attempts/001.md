---
attempt: 1
task_id: DD-10-001
started: 2026-03-21T00:00:00Z
closed: 2026-03-21T00:03:00Z
result: SUCCESS
---

## What Was Attempted

Added `useUiStore` import to `DashboardViewer.tsx`, destructured `setKiosk` from the store, and inserted a `useEffect` that calls `setKiosk(isKiosk)` whenever `isKiosk` changes and resets to `setKiosk(false)` on cleanup. This propagates the kiosk URL param (and the `kiosk` prop) to the global UI store so AppShell can hide chrome.

Applied the same pattern to `PlaylistPlayer.tsx`: imported `useUiStore`, destructured `setKiosk`, and added a `useEffect` that calls `setKiosk(true)` on mount and `setKiosk(false)` on unmount, since PlaylistPlayer always runs in kiosk mode.

## Files Modified

- `frontend/src/pages/dashboards/DashboardViewer.tsx` — added `useUiStore` import, `setKiosk` destructure, and propagation `useEffect` with cleanup
- `frontend/src/pages/dashboards/PlaylistPlayer.tsx` — added `useUiStore` import, `setKiosk` destructure, and always-kiosk `useEffect` with cleanup

## Patch Fingerprint

fingerprint: 4cd891dc2627bdc932af45a94792eb0e02b69c8216fc35c98ef544585a01afd3
before_state: bd68e76aa8cc8a7d99fb8189101da3e4ee03708e3983823235b1ba12d96891b7 (DashboardViewer HEAD) + 810c9b2fb46f0a0b5c1a8378373a6ba4319205838d67658add749a93294aa0bb (PlaylistPlayer HEAD)
after_state: 903997fd4aa8fee377e4aff78605a3143cda3125339440b9c39139d0f2871681

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit
result: PASS
output: clean (no output)

## Checklist Results

✅ DashboardViewer.tsx imports useUiStore and calls setKiosk(true) in a useEffect when isKiosk is true
✅ The same effect calls setKiosk(false) in its cleanup function (on unmount or when kiosk becomes false)
✅ AppShell.tsx sidebar is absent when ?kiosk=true — AppShell reads isKiosk from useUiStore which is now set by DashboardViewer
✅ AppShell.tsx topbar collapses to height 0 when ?kiosk=true — same mechanism
✅ Navigating away from the dashboard restores sidebar/topbar — cleanup calls setKiosk(false)

## Cycle Check

NO COLLISION — no prior fingerprint matched (none existed)

## Why This Attempt Succeeded

The fix was straightforward: DashboardViewer already had the local `isKiosk` variable but never wrote it to the global store. Adding the import, destructure, and a 3-line useEffect with cleanup fully closes the gap. PlaylistPlayer received the same treatment for defense-in-depth during loading/error states. TypeScript build passed clean.

## Notes for Next Attempt

N/A
