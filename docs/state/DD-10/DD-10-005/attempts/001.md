---
attempt: 1
task_id: DD-10-005
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:10:00Z
result: SUCCESS
---

## What Was Attempted

Added the shared `PointContextMenu` component to all 5 dashboard widget files. Each widget now wraps its point value display element in `PointContextMenu` using the children/wrapper pattern, which captures right-click events and opens a Radix DropdownMenu with point-specific actions.

- `KpiCard.tsx`: Wrapped the value display `div` (showing the numeric reading + unit) in `PointContextMenu` with `pointId=cfg.metric`, `tagName=cfg.metric`, `isAlarm=false`, `isAlarmElement=false`.
- `GaugeWidget.tsx`: Wrapped the outer return `div` (containing the EChart gauge and stale indicator) in `PointContextMenu` with `pointId=cfg.pointId`, `tagName=cfg.pointId`, `isAlarm=false`, `isAlarmElement=false`.
- `LineChart.tsx`: Added state (`contextMenuOpen`) and wrapped the chart container `div` in `PointContextMenu` using controlled open/onOpenChange props. Added `onContextMenu` handler to the container div that sets `contextMenuOpen=true`. Uses `points[0]` as the context pointId for multi-series charts (limitation documented in comments; per-series targeting requires uPlot cursor hook integration — future enhancement).
- `TableWidget.tsx`: Wrapped each dynamic row's `<tr>` in `PointContextMenu` using `row.original.point_id` as the pointId. Only dynamic rows (those backed by `cfg.pointIds`) get the context menu; static rows are passed through unchanged.
- `AlertStatusWidget.tsx`: Wrapped each alarm row's outer `<div>` in `PointContextMenu` with `pointId=alarm.source`, `tagName=alarm.source`, `isAlarm=true`, `isAlarmElement=true`.

All 5 widgets import `PointContextMenu` from `../../../shared/components/PointContextMenu`. No custom context menu logic was added to any widget.

## Files Modified

- `frontend/src/pages/dashboards/widgets/KpiCard.tsx` — Added PointContextMenu import and wrapped value display div
- `frontend/src/pages/dashboards/widgets/GaugeWidget.tsx` — Added PointContextMenu import and wrapped outer container div
- `frontend/src/pages/dashboards/widgets/LineChart.tsx` — Added useState + PointContextMenu import; wrapped chart container with controlled menu
- `frontend/src/pages/dashboards/widgets/TableWidget.tsx` — Added PointContextMenu import; wrapped each dynamic row's tr element
- `frontend/src/pages/dashboards/widgets/AlertStatusWidget.tsx` — Added PointContextMenu import; wrapped each alarm row div with isAlarm/isAlarmElement=true

## Patch Fingerprint

fingerprint: e93f89f08c7239ae7916e5cfbf7ca420bf0058efcc054faf597c1ed321fa22b9
before_state: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
after_state: b0753bfc70972f5548b4c9bb453575d9b54539c3fe23d238427713f8048b3ccc

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit 2>&1
result: PASS
output: clean (no output)

## Checklist Results

✅ KpiCard.tsx value div has onContextMenu (via PointContextMenu wrapper) that calls PointContextMenu with the correct pointId (cfg.metric)
✅ GaugeWidget.tsx container has onContextMenu (via PointContextMenu wrapper) with pointId (cfg.pointId)
✅ TableWidget.tsx rows for point-sourced data (isDynamic) have PointContextMenu per row with row.original.point_id
✅ AlertStatusWidget.tsx alarm rows have PointContextMenu with isAlarm=true and isAlarmElement=true
✅ All usages pass the canonical four props: pointId, tagName, isAlarm, isAlarmElement
✅ The shared PointContextMenu component is used — no custom menu implemented in any widget

## Cycle Check

NO COLLISION — no prior fingerprint matched (first attempt)

## Why This Attempt Succeeded

All 5 widgets were updated to import and use the shared `PointContextMenu` component. The children/wrapper pattern of the component meant wrapping the value elements directly — the component handles the right-click detection internally via a `<span style="display:contents">` wrapper and Radix DropdownMenu trigger. TypeScript build passed clean with no errors.

## Notes for Next Attempt

N/A
