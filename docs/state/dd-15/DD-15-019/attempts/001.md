---
attempt: 1
task_id: DD-15-019
started: 2026-03-26T00:00:00Z
closed: 2026-03-26T00:05:00Z
result: SUCCESS
---

## What Was Attempted

1. Seed migration: Created `migrations/20260326000004_seed_import_connections.up.sql` inserting 2 sample import connections (UUID-keyed, owned by admin user `00000000-0000-0000-0000-000000000002`):
   - `Sample REST Endpoint` (rest_api / api_key auth, enabled, last_test_status=ok) — no definitions → Delete enabled in context menu
   - `Sample Database Source` (postgres / password auth, enabled, untested) — has 1 definition → Delete grayed in context menu

2. Also seeded 1 `import_definitions` row referencing `Sample Database Source` to exercise the Delete-grayed behavior.

3. Frontend fix in `ConnectionsTab` (`Import.tsx`):
   - Added a `useQuery` for `import-definitions` (shares TanStack Query cache with `DefinitionsTab` — no duplicate network request)
   - Added `definitionCountForConnection(connectionId)` helper
   - Passed `definitionCount={definitionCountForConnection(connContextMenu.connection.id)}` to `ImportConnectionContextMenu`

4. Fixed `ImportConnectionContextMenu`:
   - Added `definitionCount: number` prop
   - Replaced `const hasDefinitions = false` hardcode with `const hasDefinitions = definitionCount > 0`
   - Added `role="menu"` to the menu div so `[role="menu"]` selector works in UAT

## Files Modified

- `frontend/src/pages/settings/Import.tsx`: added definitionCount prop, definitions query, helper function, role="menu"
- `migrations/20260326000004_seed_import_connections.up.sql`: new file — 2 connections + 1 definition
- `migrations/20260326000004_seed_import_connections.down.sql`: new file — reverse seed

## Changed Files

before_state: aa8e7a90e116cc5c646f6a2c831aad005a11d285
changed_files:
  - frontend/src/pages/settings/Import.tsx
  - migrations/20260326000004_seed_import_connections.up.sql (untracked new)
  - migrations/20260326000004_seed_import_connections.down.sql (untracked new)

changed_functions: (for human review only)
  - definitionCountForConnection
  - ImportConnectionContextMenu (modified — added definitionCount prop)

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit; echo "EXIT: $?"
result: PASS
output: EXIT: 0

command: cd /home/io/io-dev/io/frontend && pnpm build
result: PASS
output: BUILD_EXIT:0 — built in 11.13s

command: cd /home/io/io-dev/io/frontend && pnpm test
result: PASS (pre-existing failures only)
output: 2 failed | 483 passed — failures are permissions count test (pre-existing, unrelated to Import.tsx)

command: npx madge --circular src/
result: PASS
output: No circular dependency found!

command: git diff HEAD -- frontend/src/pages/settings/Import.tsx | grep "^+" | grep -iE "TODO|FIXME|stub"
result: PASS
output: (no matches)

## Checklist Results

✅ Right-clicking a connection row on /settings/import (Connections tab) shows a context menu — seed adds 2 rows; onContextMenu handler wired on every row; renders ImportConnectionContextMenu
✅ Menu items: "Test Connection", "Enable"/"Disable", "Delete" all present — verified in ImportConnectionContextMenu JSX (lines 691-699)
✅ "Delete" is grayed/disabled when definitions reference the connection — definitionCount prop now computed from actual definitions data; Sample Database Source has 1 definition
✅ Context menu dismisses on Escape and outside-click — useContextMenuDismiss handles both (lines 596-613)
✅ role="menu" on menu div — added to ImportConnectionContextMenu container

## Cycle Check

NO COLLISION — no prior attempts for this task.

## Why This Attempt Succeeded

All three issues resolved:
1. No seed data — fixed by migration adding 2 sample connections + 1 definition
2. Context menu wiring was already present; confirmed and verified
3. Delete graying was hardcoded false — fixed by fetching definitions and computing per-connection count

## Notes for Next Attempt

N/A
