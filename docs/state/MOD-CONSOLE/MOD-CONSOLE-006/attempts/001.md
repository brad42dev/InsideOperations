---
attempt: 1
task_id: MOD-CONSOLE-006
started: 2026-03-21T23:29:09Z
closed: 2026-03-21T23:32:00Z
result: SUCCESS
---

## What Was Attempted

Replaced the hand-rolled local `ContextMenu` block in `GraphicPane.tsx` (lines 614-664) with the shared `PointContextMenu` component. Extended `pointCtxMenu` state to include `tagName`, `isAlarm`, and `isAlarmElement`. Updated `handleSvgContextMenu` to populate these new fields from the `tooltipValuesRef` cache. Added permission gating for "Investigate Point" (`forensics:read`) and "Report on Point" (`reports:read`) in `PointContextMenu.tsx`, and fixed "Investigate Alarm" to use `isAlarm || isAlarmElement`.

## Files Modified

- `frontend/src/pages/console/panes/GraphicPane.tsx`:
  - Removed `ContextMenu` import; added `PointContextMenu` import
  - Removed unused `useNavigate` import and `navigate` declaration
  - Extended `pointCtxMenu` state type with `tagName: string`, `isAlarm: boolean`, `isAlarmElement: boolean`
  - Updated `handleSvgContextMenu` to resolve `tagName` (falls back to `pointId`) and `isAlarm` from `tooltipValuesRef`
  - Replaced local 6-item `ContextMenu` block with `<PointContextMenu>` using a fixed-position portal anchor span

- `frontend/src/shared/components/PointContextMenu.tsx`:
  - Added `usePermission` import
  - Added `isAlarmElement` to destructured props (was in interface but not destructured)
  - Added `canForensics = usePermission('forensics:read')` and `canReports = usePermission('reports:read')`
  - Added `handleReportOnPoint` callback navigating to `/reports?point=...`
  - Fixed "Investigate Alarm" condition from `isAlarm` to `isAlarm || isAlarmElement`
  - Gated "Investigate Point" item on `canForensics`
  - Added "Report on Point" item gated on `canReports`

## Patch Fingerprint

fingerprint: 8e7d00b018ebd2bd9bc778fac40ce02a8fe68443f8b5bbce408897e14572d72d
before_state: (clean working tree — no prior diff)
after_state: 7e0f751f9d398445ed9748afb17ae76b964cd16d8672766a0d7f5c1420a99b4b

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit
result: PASS
output: clean (no output)

## Checklist Results

✅ GraphicPane.tsx does NOT import ContextMenu for point right-click — ContextMenu import removed
✅ GraphicPane.tsx imports and uses PointContextMenu for point right-click
✅ pointCtxMenu state stores { x, y, pointId, tagName, isAlarm, isAlarmElement }
✅ tagName is populated (set to pointId as canonical fallback — WsPointValue has no tagName field)
✅ isAlarm / isAlarmElement passed to PointContextMenu
✅ "Investigate Alarm" only appears when isAlarm || isAlarmElement is true
✅ "Investigate Point" gated on forensics:read; "Report on Point" gated on reports:read

## Cycle Check

NO COLLISION — no prior fingerprint existed (attempt 1)

## Why This Attempt Succeeded

All four spec requirements were addressed: state extension, handler population, component replacement, and permission gating. TypeScript compiled clean after removing the unused `navigate` import that was previously only used by the now-deleted local menu.

## Notes for Next Attempt

N/A
