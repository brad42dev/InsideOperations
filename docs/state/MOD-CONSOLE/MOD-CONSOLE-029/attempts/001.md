---
attempt: 1
task_id: MOD-CONSOLE-029
started: 2026-03-26T10:00:00Z
closed: 2026-03-26T10:15:00Z
result: SUCCESS
---

## What Was Attempted

Added two missing save-feedback features to the Console module's `ConsolePage` component in `frontend/src/pages/console/index.tsx`:

1. **Dirty indicator**: Added `isDirty` boolean state. Set to `true` inside `scheduleSave` (when a debounced save is queued). Set to `false` inside the `saveMutation.onSuccess` path after a confirmed successful save. Rendered as a 6px amber dot (`var(--io-warning)`) after the workspace name on the active tab only (`isDirty && ws.id === activeId`).

2. **Persistent failure banner fixes**:
   - Removed the dismissible toast on first failure ("Failed to save workspace. Retrying…") from both `onSuccess` (false-success path) and `onError` paths.
   - Removed the extra toast on third failure ("Workspace save failed after multiple attempts.") from both paths.
   - Fixed banner text from "Workspace changes not saved. Retry?" to "Workspace changes not saved" (the "Save now" button was already present and correct).
   - The banner remains non-dismissible (no close button), appears after 3 consecutive failures, and disappears only after a successful save.

## Files Modified

- `frontend/src/pages/console/index.tsx`: Added `isDirty` state, wired it to `scheduleSave` and save success, added dirty dot to tab render, removed erroneous failure toasts, fixed banner text.

## Changed Files

before_state: 6d170b3e4f2ca580359bbb47d9fd967523d0e3b0
changed_files:
  - frontend/src/pages/console/index.tsx

changed_functions: (for human review only — not used in automated cycle detection)
  - isDirty (useState)
  - scheduleSave (modified — adds setIsDirty(true))
  - saveMutation.onSuccess (modified — adds setIsDirty(false), removes toasts)
  - saveMutation.onError (modified — removes toasts)

## Verification

command: cd /tmp/io-worktrees/MOD-CONSOLE-029/frontend && npx tsc --noEmit
result: PASS
output: clean (0 errors)

command: pnpm test
result: PASS (pre-existing failures only)
output: 2 failed (permissions.test.ts — pre-existing, unrelated to console), 477 passed

command: pnpm build
result: PASS
output: BUILD_EXIT:0, built in 13.15s

## Checklist Results

✅ Modify workspace layout → dot/asterisk visible on tab within 2s — isDirty set true on scheduleSave call, dot rendered on active tab
✅ After save completes → dot disappears — setIsDirty(false) on saveMutation success
✅ After 3 failures → persistent banner appears (not just a toast) — showSaveBanner set after saveFailCountRef >= 3, no more toasts
✅ Banner has "Save now" button that triggers retry — already present, now sole error feedback
✅ Banner persists until a successful save — setShowSaveBanner(false) only on success

## Cycle Check

NO COLLISION — no prior attempts for this task.

## Why This Attempt Succeeded

All five acceptance criteria met. The changes were minimal and targeted: 3 state wiring changes + 1 JSX addition + 4 lines of toasts removed + 1 string fix. TypeScript clean, build clean, no regressions introduced.

## Notes for Next Attempt

N/A
