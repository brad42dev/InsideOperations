---
attempt: 1
task_id: MOD-CONSOLE-035
started: 2026-03-26T00:00:00Z
closed: 2026-03-26T00:05:00Z
result: SUCCESS
---

## What Was Attempted

Removed two UI guards in `frontend/src/pages/console/index.tsx` that prevented deleting the last workspace:

1. Line 1365: unwrapped the toolbar delete button from `{workspaces.length > 1 && (...)}` — the button is now always rendered.
2. Line 1893: removed `disabled: workspaces.length <= 1` from the context-menu Delete item — the item is now always enabled.

No other code was changed. The underlying `deleteActiveWorkspace()` function was not modified.

## Files Modified

- `frontend/src/pages/console/index.tsx`: removed `workspaces.length > 1 &&` conditional wrapper (toolbar delete button) and `disabled: workspaces.length <= 1` property (context-menu Delete item).

## Changed Files

before_state: 928c63657d1f10374952f9a20f4de6dcb9eebb6d
changed_files:
  - frontend/src/pages/console/index.tsx

changed_functions: (for human review only — not used in automated cycle detection)
  - none (no new functions; only removed conditional guards)

## Verification

command: cd /tmp/io-worktrees/MOD-CONSOLE-035/frontend && npx tsc --noEmit
result: PASS
output: clean (0 errors)

command: pnpm build
result: PASS
output: BUILD_EXIT:0

command: pnpm test
result: PASS (pre-existing failures only)
output: 2 failed tests in permissions.test.ts (reports:generate, reports:schedule_manage) — unrelated to this task, pre-existing

command: npx madge --circular src/
result: PASS
output: No circular dependency found!

## Checklist Results

- When exactly one workspace exists, the toolbar delete button is visible and clickable — button rendered unconditionally
- When exactly one workspace exists, the context-menu "Delete" item is enabled — disabled condition removed
- Deleting the last workspace navigates to the zero-workspace empty state — setActiveId(nextWorkspaces[0]?.id ?? null) sets to null when array is empty, existing empty-state path handles this
- No regression: deleting non-last workspaces still works correctly — logic unchanged, only guards removed

## Cycle Check

NO COLLISION — no prior attempts exist for this task.

## Why This Attempt Succeeded

Two targeted one-line removals. TypeScript compilation clean. Production build clean. No new TODOs or stubs. No circular imports.

## Notes for Next Attempt

N/A
