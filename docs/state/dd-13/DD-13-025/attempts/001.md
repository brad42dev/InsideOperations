---
attempt: 1
task_id: DD-13-025
started: 2026-03-26T10:00:00Z
closed: 2026-03-26T10:12:00Z
result: SUCCESS
---

## What Was Attempted

Fixed the `create_instance` handler in `services/api-gateway/src/handlers/logs.rs` by changing the status parameter binding from `"pending"` to `"draft"`. Added `tracing::error!` logging to expose the actual database error on failure. Added `use tracing::error;` import.

## Files Modified

- `services/api-gateway/src/handlers/logs.rs`: Changed `.bind("pending")` to `.bind("draft")` on line 595 (now 596). Added `use tracing::error;` import. Added detailed error logging block in the `Err(e)` match arm.

## Changed Files

before_state: 19d6e59e755bf7a0dc3894d3975198820336350d
changed_files:
  - services/api-gateway/src/handlers/logs.rs

changed_functions: (for human review only — not used in automated cycle detection)
  - create_instance (modified, not new)

## Verification

command: cargo check -p api-gateway && cargo test -p api-gateway
result: PASS
output:
  - cargo check: 0 errors, 12 pre-existing warnings, Finished dev profile
  - cargo test: 29 passed; 0 failed (expressions tests), 5 passed; 0 failed; 3 ignored (security tests)

## Checklist Results

- POST /api/logs/instances accepts valid CreateInstanceRequest: status now binds "draft" which satisfies the DB CHECK constraint — ✅
- Returns HTTP 201 CREATED with LogInstanceRow response: StatusCode::CREATED, ApiResponse::ok(instance) — ✅
- Instance is persisted to database with correct status="draft": .bind("draft") — ✅
- template_id foreign key is satisfied: template existence check runs before insert (lines 569-582) — ✅
- Proper error response on validation failure (400/404, not 500): Forbidden → 403, NotFound → 404, only actual DB errors → 500 — ✅
- Detailed logging on DB error: tracing::error! logs template_id and error — ✅

## Cycle Check

NO COLLISION — no prior attempts for this task.

## Why This Attempt Succeeded

Root cause was migration `20260322000002_log_instance_status_states.up.sql` which updated the `log_instances.status` CHECK constraint from `('pending', 'in_progress', 'completed')` to `('draft', 'in_progress', 'submitted', 'reviewed')`. The handler still bound `"pending"` as the status value, which violates the new constraint. PostgreSQL rejects the INSERT with a check constraint violation, sqlx returns an Err, and the handler returns HTTP 500 "A database error occurred". Changing the bound value to `"draft"` resolves the violation.

## Notes for Next Attempt

N/A
