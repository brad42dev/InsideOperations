---
attempt: 1
task_id: GFX-SHAPES-007
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:10:00Z
result: SUCCESS
---

## What Was Attempted

Fixed `services/api-gateway/src/seed_shapes.rs` to use canonical shape IDs instead of per-variant IDs. Replaced each pair of opt1/opt2 rows with a single canonical row whose sidecar JSON encodes variants via `variants.options`. Fixed heat-transfer shape IDs and sidecar category fields from `"heat-exchange"` to `"heat-transfer"`.

Specific changes:
- Replaced `pump-centrifugal-opt1` + `pump-centrifugal-opt2` rows with single `pump-centrifugal` row, sidecar has `variants.options` with opt1/opt2 file references
- Replaced `compressor-opt1` + `compressor-opt2` rows with single `compressor` row, sidecar has `variants.options`
- Replaced `fan-blower-opt1` + `fan-blower-opt2` rows with single `fan-blower` row, sidecar has `variants.options`
- Replaced `motor-opt1` + `motor-opt2` rows with single `motor` row, sidecar has `variants.options`
- Renamed `hx-shell-tube-opt1` → `heat-exchanger-shell-tube`, updated sidecar `"id"` and `"category"` fields
- Renamed `hx-plate-opt1` → `heat-exchanger-plate`, updated sidecar `"id"` and `"category"` fields
- Renamed `heater-fired-opt1` → `heater-fired`, updated sidecar `"id"` and `"category"` fields
- Also fixed `air-cooler` sidecar `"category"` from `"heat-exchange"` to `"heat-transfer"` (same bug)

## Files Modified

- `services/api-gateway/src/seed_shapes.rs` — removed 7 per-variant rows, added 7 canonical rows with variants.options sidecars; fixed 4 heat-transfer sidecar id/category fields

## Patch Fingerprint

fingerprint: 793ff8f699ee8ecc9eaaa90159ac2f48c33130532c0e87db86e0892af95f43ea
before_state: c1f025775c6c298dd3d923c79ff3a7a680cd08726790e4110d893181cda3c51d
after_state: ad4a1a9203421f0a9985042ae26d80b075023df41979ae84ba0dca8e05748b0d

## Verification

command: cargo build -p api-gateway
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 3.05s (10 warnings, pre-existing)

## Checklist Results

✅ seed_shapes.rs seeds pumps as shape_id: "pump-centrifugal" (canonical) with sidecar variants.options
✅ seed_shapes.rs seeds rotating equipment as shape_id: "compressor", "fan-blower", "motor" (not opt1/opt2 variants)
✅ seed_shapes.rs seeds heat-transfer shapes as shape_id: "heat-exchanger-shell-tube", "heat-exchanger-plate"
✅ seed_shapes.rs category field for heat-transfer shapes is "heat-transfer" (not "heat-exchange")
✅ batch_shapes handler returns shape when called with ["pump-centrifugal"] — seeded row now has canonical ID
✅ seed_shapes.rs does NOT create separate rows for opt1 and opt2 — single canonical row with sidecar variants.options

## Cycle Check

NO COLLISION — no prior fingerprint matched (N=1, first attempt)

## Why This Attempt Succeeded

All per-variant IDs were replaced with canonical IDs. The two-row opt1/opt2 pattern was collapsed to one row per equipment type. Heat-transfer shape IDs were renamed and sidecar JSON id/category fields updated to match. Build passes cleanly.

## Notes for Next Attempt

N/A
