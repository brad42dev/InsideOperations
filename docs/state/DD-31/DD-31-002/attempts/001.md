---
attempt: 1
task_id: DD-31-002
started: 2026-03-22T10:00:00Z
closed: 2026-03-22T10:20:00Z
result: SUCCESS
---

## What Was Attempted

Added a separate `sendEmergency()` API method to `notifications.ts` that POSTs to `/api/notifications/send-emergency`. Updated `SendAlertPanel` in `alerts/index.tsx` to add a `sendEmergencyMutation` wrapping the new method, then split `doSend()` to call `sendEmergencyMutation` when `severity === 'emergency'` and `sendMutation` otherwise. Filtered the severity button list with `.filter((s) => s !== 'emergency' || canSendEmergency)` using `usePermission('alerts:send_emergency')`. Applied the same split pattern and permission filter to `AlertComposer.tsx` (both `handleSubmit` and the `SEVERITIES` array, plus the Emergency Quick-Send template section).

## Files Modified

- `frontend/src/api/notifications.ts` — added `sendEmergency(payload)` method posting to `/api/notifications/send-emergency`
- `frontend/src/pages/alerts/index.tsx` — added `canSendEmergency` via `usePermission`, added `sendEmergencyMutation`, split `doSend()` on severity, filtered 'emergency' severity button and updated send button/success states to track active mutation
- `frontend/src/pages/alerts/AlertComposer.tsx` — added `usePermission` import, derived filtered `SEVERITIES` from `ALL_SEVERITIES`, added `sendEmergencyMutation`, split `handleSubmit` on severity, filtered Emergency Quick-Send section, updated submit button/error display to track active mutation

## Patch Fingerprint

fingerprint: 39908800cc002b5e95e014d6dc7d6cb919d1c6bc3f5569995cbea1d1b7d4891a
before_state: (no stash, no prior state baseline)
after_state: 0f1f55439273552a8048d73eae74813802561714c0e2803cabeac3310bf0a913

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit
result: PASS
output: clean (no output)

## Checklist Results

✅ `notificationsApi` has a separate `sendEmergency(payload)` method that POSTs to `/api/notifications/send-emergency`
✅ When `severity === 'emergency'`, `doSend` calls `sendEmergency` instead of `sendNotification`
✅ The EMERGENCY severity option is hidden (not shown) when the user lacks `alerts:send_emergency` permission
✅ Alert composer (AlertComposer.tsx) applies the same separation

## Cycle Check

NO COLLISION — no prior fingerprint matched (first attempt)

## Why This Attempt Succeeded

All four checklist items implemented and verified. TypeScript build is clean. The approach added a new API method, a new mutation, split the send path on severity, and used the existing `usePermission` hook to filter the emergency option from both UI surfaces.

## Notes for Next Attempt

N/A
