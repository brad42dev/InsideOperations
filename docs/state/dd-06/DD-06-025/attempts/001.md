---
attempt: 1
task_id: DD-06-025
started: 2026-03-26T00:00:00Z
closed: 2026-03-26T00:05:00Z
result: SUCCESS
---

## What Was Attempted

Added WebSocket subscription support for unacknowledged alarm count in the AppShell alert badge. Changes were made in two files:

1. `frontend/src/shared/hooks/useWsWorker.ts` — Added alarm event types (`AlarmCountUpdate`, `AlarmCreatedEvent`, `AlarmAcknowledgedEvent`), handler sets, routing in `handleWorkerMessage` for `alarm_count_update`, `alarm_created`, and `alarm_acknowledged` messages, public API methods (`onAlarmCountUpdate`, `onAlarmCreated`, `onAlarmAcknowledged`), and cleanup in `disconnect()`.

2. `frontend/src/shared/layout/AppShell.tsx` — Rewrote `useUnacknowledgedAlertCount()` to remove `refetchInterval: 30_000`, keep the REST fetch as a bootstrap (with `staleTime: Infinity`), add `useState<number | undefined>` for a live count, seed it via `useEffect` from the bootstrap, and subscribe to WS alarm events in a second `useEffect` using the three new `wsManager.on*` methods.

## Files Modified

- `frontend/src/shared/hooks/useWsWorker.ts` — Added alarm event types, handler sets, message routing, public API, and disconnect cleanup
- `frontend/src/shared/layout/AppShell.tsx` — Replaced polling-based `useUnacknowledgedAlertCount()` with WS-driven implementation keeping REST bootstrap

## Changed Files

before_state: 130473616ea26d728fd86ebd65eb022d460f994c
changed_files:
  - frontend/src/shared/hooks/useWsWorker.ts
  - frontend/src/shared/layout/AppShell.tsx

changed_functions: (for human review only — not used in automated cycle detection)
  - alarmCountUpdateHandlers (const)
  - alarmCreatedHandlers (const)
  - alarmAcknowledgedHandlers (const)
  - onAlarmCountUpdate (method on wsWorkerConnector)
  - onAlarmCreated (method on wsWorkerConnector)
  - onAlarmAcknowledged (method on wsWorkerConnector)
  - useUnacknowledgedAlertCount (updated hook in AppShell.tsx)

## Verification

command: cd /tmp/io-worktrees/DD-06-025/frontend && npx tsc --noEmit
result: PASS
output: clean (no errors)

command: cd /tmp/io-worktrees/DD-06-025/frontend && pnpm build
result: PASS
output: BUILD_EXIT:0

command: cd /tmp/io-worktrees/DD-06-025/frontend && pnpm test
result: PASS (2 pre-existing failures unrelated to this task in permissions.test.ts — confirmed pre-existing by stash test)
output: 483 tests pass, 2 pre-existing failures

command: npx madge --circular src/ (via main repo frontend)
result: PASS
output: No circular dependency found!

## Checklist Results

- The alert count updates within 1-2 seconds of a new alarm arriving (not waiting 30s) — WS subscription via onAlarmCreated/onAlarmCountUpdate now updates liveCount immediately on broker push
- The count decrements within 1-2 seconds of an alarm being acknowledged — WS subscription via onAlarmAcknowledged/onAlarmCountUpdate updates immediately
- No 30-second polling interval in useUnacknowledgedAlertCount() — refetchInterval removed, staleTime set to Infinity
- A WebSocket message handler updates the count on alarm_created or alarm_count_changed events — alarm_count_update, alarm_created, alarm_acknowledged all handled in useWsWorker.ts handleWorkerMessage and routed to AppShell hook

## Cycle Check

NO COLLISION — no prior attempts exist

## Why This Attempt Succeeded

The wsWorker SharedWorker already broadcasts all broker messages to connected tab ports (line 223: `broadcast(msg)`). No changes to the worker itself were needed. Adding handler sets and routing in `useWsWorker.ts` was sufficient to expose the alarm events via a typed public API. AppShell then subscribes to these in a useEffect with proper cleanup.

## Notes for Next Attempt

N/A
