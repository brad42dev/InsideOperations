---
attempt: 1
task_id: DD-26-006
started: 2026-03-22T10:00:00Z
closed: 2026-03-22T10:15:00Z
result: SUCCESS
---

## What Was Attempted

Auto-detect model domain from manifest.json inside .iomodel ZIP rather than accepting it from the multipart form.

Three files were changed:

1. `services/recognition-service/src/main.rs` — The existing `unwrap_or_else(|| "pid".to_string())` fallback on `manifest.model_domain` was replaced with strict validation: accepts only `"pid"` or `"dcs"`, rejects anything else (including None/missing) with a `400 Bad Request` and a clear error message. The handler already read domain exclusively from manifest.json — no `"domain"` form field handling existed to remove.

2. `frontend/src/api/recognition.ts` — Removed the `domain: string` parameter from `uploadModel`. Removed `form.append('domain', domain)` from the FormData construction.

3. `frontend/src/pages/settings/Recognition.tsx` — Removed `const [uploadDomain, setUploadDomain] = useState<'pid' | 'dcs'>('pid')`. Updated `recognitionApi.uploadModel(file, uploadDomain)` to `recognitionApi.uploadModel(file)`. Removed the `<select>` domain dropdown from the upload controls. Updated the success toast to show the auto-detected domain and version from the response. Updated the helper text to indicate domain is auto-detected.

## Files Modified

- `services/recognition-service/src/main.rs` — replaced unwrap_or fallback with strict pid/dcs validation with clear error messages
- `frontend/src/api/recognition.ts` — removed domain param and form field from uploadModel
- `frontend/src/pages/settings/Recognition.tsx` — removed uploadDomain state, select dropdown, updated call site and success toast

## Patch Fingerprint

fingerprint: c4df863445aa0aaa07b96d31eeac42f046aa2d1c3958f766d9dd356770d649fe
before_state: 3a86803b6525b5eb999abd9ebdcf38713cdaec5f66a493f0e21337336d0e26ae
after_state: d06f5c48dac571fb13b6d8294cff6c556b80195b6817773b047c1a94a2efc454

## Verification

command: cd /home/io/io-dev/io && cargo check -p recognition-service && cd frontend && npx tsc --noEmit
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.73s / TS clean (no output)

## Checklist Results

✅ upload_model in main.rs no longer reads a "domain" field from the multipart form — the match arm for "domain" was already absent; handler only processes "file"
✅ domain is read exclusively from manifest.json → model_domain key inside the ZIP — strict match on manifest.model_domain.as_deref()
✅ If manifest.json is missing or model_domain is not "pid" or "dcs", upload is rejected with clear error — BadRequest with descriptive messages for both None and invalid-string cases; missing manifest.json already rejected by parse_and_verify_iomodel
✅ Frontend recognitionApi.uploadModel signature no longer accepts a domain parameter — signature is now uploadModel(file: File)
✅ Settings > Recognition UI removes the domain selector dropdown — <select> element and uploadDomain state removed

## Cycle Check

NO COLLISION — no prior fingerprint matched (attempt 1, no prior fingerprints)

## Why This Attempt Succeeded

All three files had straightforward, targeted changes. The backend already had the ZIP parsing infrastructure in place; only the domain validation logic needed strengthening. The frontend changes were purely removals. Both build checks passed cleanly on first attempt.

## Notes for Next Attempt

N/A
