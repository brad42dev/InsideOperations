---
attempt: 1
task_id: DD-11-002
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:10:00Z
result: SUCCESS
---

## What Was Attempted

Added `typst-as-lib` (v0.15, with `typst-kit-fonts` + `typst-kit-embed-fonts` features), `typst-pdf-render` (aliased from `typst-pdf` v0.14), and `typst-engine` (aliased from `typst` v0.14) as optional dependencies to `services/api-gateway/Cargo.toml`. Updated the `typst-pdf` feature flag to include all three via `dep:` syntax.

Updated `compile_typst_pdf()` in `report_generator.rs` to use the correct typst-as-lib 0.15.x API: `TypstEngine::builder().main_file(source).build()` then `.compile::<PagedDocument>()` returning a `Warned<Result<PagedDocument>>`, followed by `typst_pdf_render::pdf(&doc, &PdfOptions::default())` to produce PDF bytes.

Added a `"pdf"` arm to `format_to_content_type()` in `handlers/reports.rs` returning `("application/pdf", "pdf")` so the download response has the correct MIME type.

## Files Modified

- `services/api-gateway/Cargo.toml` — Added typst-as-lib 0.15, typst-pdf-render (typst-pdf 0.14), typst-engine (typst 0.14) as optional deps; updated typst-pdf feature to list all three
- `services/api-gateway/src/report_generator.rs` — Fixed `compile_typst_pdf()` from broken `.compile_pdf()` call to correct API using `.compile::<PagedDocument>()` + `typst_pdf_render::pdf()`
- `services/api-gateway/src/handlers/reports.rs` — Added `"pdf" => ("application/pdf", "pdf")` arm to `format_to_content_type()`

## Patch Fingerprint
fingerprint: 2de95af25c525acf60ac93cd35d4d574d3d2342937926c6a342a8fc98010258e
before_state: 686e40e8f1197fb39e730a819b2c4664225b0876bdd425ef50e39ee64e0a0838
after_state: 77e4411b1196737ecbfcc2b54bbd6ab2c9ce83b4047128c83dd6de19e4953de1

## Verification
command: cargo check -p api-gateway --features typst-pdf
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.19s

## Checklist Results
✅ `typst-as-lib` appears in `[dependencies]` section of Cargo.toml (line 52, version "0.15")
✅ `typst-pdf` feature in Cargo.toml includes `typst-as-lib` in dependency list via `dep:typst-as-lib`
✅ `compile_typst_pdf()` exists and calls `TypstEngine::builder().compile::<PagedDocument>()` + `typst_pdf_render::pdf()`
✅ `generate_pdf_report()` returns actual PDF bytes when typst-pdf feature is enabled (calls compile_typst_pdf and returns its Ok(bytes))
✅ `format_to_content_type()` returns `"application/pdf"` for the `"pdf"` format key

## Cycle Check
NO COLLISION — this is attempt 1, no prior fingerprints to compare against.

## Why This Attempt Succeeded

The existing code already had the correct structure (compile_typst_pdf function, feature gating in generate_pdf_report, build_typst_template). The issues were:
1. typst-as-lib wasn't in Cargo.toml — fixed by adding it
2. compile_typst_pdf used `.compile_pdf()` which doesn't exist in v0.15 — fixed to use correct `.compile::<PagedDocument>()` + `typst_pdf_render::pdf()` API
3. format_to_content_type had no "pdf" arm — fixed by adding it
4. Needed to alias typst-pdf and typst crates to avoid name collision with the feature flag

## Notes for Next Attempt
N/A
