---
attempt: 1
task_id: MOD-PROCESS-018
started: 2026-03-26T10:00:00Z
closed: 2026-03-26T10:15:00Z
result: SUCCESS
---

## What Was Attempted

Replaced all hardcoded hex color values and font-family strings in the Process module with CSS design tokens, specifically:

1. `frontend/src/index.css` — Added `--io-font-mono` CSS custom property to the `:root` block (static across all themes, matching the `.io-mono` class font stack already defined in the file).

2. `frontend/src/pages/process/index.tsx` — Four substitutions:
   - `connectedDot` object (lines ~1060-1064): `#22C55E` → `var(--io-success)`, `#EAB308` → `var(--io-warning)`, `#EF4444` → `var(--io-danger)`
   - Tooltip `fontFamily` (line ~1286): `'JetBrains Mono, monospace'` → `'var(--io-font-mono)'`
   - Tooltip quality color (line ~1290): `#22C55E`/`#EF4444`/`#F59E0B` → `var(--io-success)`/`var(--io-danger)`/`var(--io-warning)`
   - Historical mode button (lines ~1411-1412): `#F59E0B`/`#78350f22` → `var(--io-warning)`/`var(--io-warning-subtle)`

3. `frontend/src/pages/process/ProcessMinimap.tsx` — Canvas background (line ~115): `ctx.fillStyle = '#1a1f2e'` → `getComputedStyle(document.documentElement).getPropertyValue('--io-surface-primary').trim() || '#09090B'`

## Files Modified

- `frontend/src/index.css`: Added `--io-font-mono` token to `:root` block
- `frontend/src/pages/process/index.tsx`: Replaced 6 hardcoded color/font values with CSS tokens
- `frontend/src/pages/process/ProcessMinimap.tsx`: Canvas background resolved via getComputedStyle at draw time

## Changed Files

before_state: 1cbac8964708d8d562af9d54f647b947b29fb3e0
changed_files:
  - frontend/src/index.css
  - frontend/src/pages/process/ProcessMinimap.tsx
  - frontend/src/pages/process/index.tsx

changed_functions: (for human review only)
  - bgColor (const in ProcessMinimap useEffect draw callback)

## Verification

command: cd frontend && npx tsc --noEmit; echo EXIT:$?
result: PASS
output: EXIT:0 (0 errors, baseline was also 0)

command: cd frontend && pnpm build
result: PASS
output: BUILD_EXIT:0, built in 10.60s

command: cd frontend && pnpm test
result: PASS (pre-existing failures only)
output: 2 failed | 477 passed — failures in permissions.test.ts (pre-existing, unrelated to Process module)

command: npx madge --circular src/
result: PASS
output: No circular dependency found!

## Checklist Results

✅ Connection status indicator uses token colors (var(--io-success), var(--io-warning), var(--io-danger)) not hex literals
✅ Tooltip monospace font uses var(--io-font-mono) — token added to index.css :root block
✅ Minimap canvas background resolved via getComputedStyle at draw time, not hardcoded #1a1f2e
✅ No new TypeScript errors introduced (delta: 0)
✅ Production build passes (BUILD_EXIT:0)
✅ No new TODO/FIXME stubs introduced
✅ No circular imports
✅ Alarm/ISA-101 colors NOT touched

## Cycle Check

NO COLLISION — no prior attempts exist for this task.

## Why This Attempt Succeeded

All three locations specified in the task were fixed correctly:
- CSS vars are used directly in inline React styles (string interpolation via `'var(--token)'` syntax)
- The minimap canvas workaround correctly uses getComputedStyle since canvas 2D context cannot resolve CSS variables natively
- The `--io-font-mono` token was added to the CSS token registry since it did not previously exist, matching the existing `.io-mono` class font stack

## Notes for Next Attempt

N/A
