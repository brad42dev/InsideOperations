---
attempt: 3
task_id: MOD-PROCESS-016
started: 2026-03-26T00:05:00Z
closed: 2026-03-26T00:08:00Z
result: SUCCESS
---

## What Was Attempted

Created `frontend/src/pages/process/ProcessDetachedView.tsx` — a fully functional detached window component that:
- Reads `:viewId` from `useParams()` and loads the graphic via TanStack Query
- Maintains its own independent viewport state (panX, panY, zoom, canvasWidth, canvasHeight, screenWidth, screenHeight)
- Maintains its own binding index (flat + RBush), debounced visible point IDs, and WebSocket subscription set via `useWebSocketRaf`
- Renders a thin 32px title bar with: connection status dot, view name, zoom%, zoom-in/out/fit buttons, minimap toggle, fullscreen button, and live clock
- Renders the full SceneRenderer and ProcessMinimap below the title bar — no sidebar, no module switcher, no AppShell
- Supports keyboard shortcuts: arrow key pan, +/- zoom, M for minimap, Ctrl+0 zoom-to-fit

Updated `frontend/src/App.tsx`:
- Added lazy import for ProcessDetachedView
- Changed `/detached/process/:viewId` route from `<ProcessView detached />` to `<ProcessDetachedView />`

## Files Modified

- `frontend/src/pages/process/ProcessDetachedView.tsx` — NEW FILE: full detached window implementation
- `frontend/src/App.tsx` — added lazy import + updated route element

## Changed Files

before_state: 1cbac8964708d8d562af9d54f647b947b29fb3e0
changed_files:
  - frontend/src/App.tsx
  - frontend/src/pages/process/ProcessDetachedView.tsx

changed_functions: (for human review only)
  - ProcessDetachedView (default export)
  - useClock (internal hook)
  - buildBindingIndex, queryFlatIndex, queryRBushIndex, buildRBushIndex, getBufferFraction (spatial index helpers)
  - zoomIn, zoomOut, zoomFit, toggleFullscreen (controls)

## Verification

command: cd frontend && npx tsc --noEmit
result: PASS
output: clean (0 errors, delta=0 from baseline of 0)

command: cd frontend && pnpm test
result: PASS (task files)
output: 2 pre-existing failures in permissions.test.ts (reports:generate, reports:schedule_manage) — not in any file this task touched. 477 other tests pass.

command: cd frontend && pnpm build
result: PASS
output: BUILD_EXIT:0, built in 10.40s

command: npx madge --circular src/
result: PASS
output: No circular dependency found!

## Checklist Results

- Route `/detached/process/:viewId` defined in App.tsx renders ProcessDetachedView: ✅
- Component loads graphic by viewId from useParams (not selectedId state): ✅
- Thin title bar: view name, connection status dot, live clock: ✅
- Basic controls: zoom-in, zoom-out, zoom-to-fit, minimap toggle, fullscreen: ✅
- No sidebar, no module switcher, no AppShell chrome: ✅
- Viewport, LOD, and subscriptions are independent per window (own useState + useWebSocketRaf): ✅
- Real-time subscriptions via useWebSocketRaf (SharedWorker de-duplicates across windows): ✅

## Cycle Check

NO COLLISION — prior attempts 1 and 2 had no changed files (ZOMBIE). This attempt has a different changed_files set.

## Why This Attempt Succeeded

Implementation was straightforward: the main ProcessPage (index.tsx) already had all the logic needed. Extracted the viewport management, spatial binding index, LOD computation, and WebSocket subscription logic into a self-contained component that renders the minimal shell spec §11.2 requires. TypeScript was clean on the first pass except one unused variable (viewportAnimRef from animateViewportTo that was included but not needed in the detached view — removed immediately). Build and circular import checks all passed.

## Notes for Next Attempt

N/A
