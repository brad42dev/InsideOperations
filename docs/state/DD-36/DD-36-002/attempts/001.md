---
attempt: 1
task_id: DD-36-002
started: 2026-03-23T00:00:00Z
closed: 2026-03-23T00:02:00Z
result: SUCCESS
---

## What Was Attempted

Added `path` label (route template) to both `io_http_requests_total` and `io_http_request_duration_seconds` metrics in `services/api-gateway/src/mw.rs`.

Also moved `metrics_middleware` from the outermost app layer (after `with_state`) into the `api` router block (before `with_state`) in `services/api-gateway/src/main.rs`, so that Axum's routing pass has already populated `MatchedPath` in request extensions when the middleware runs.

Specific changes:
- `mw.rs`: Added `MatchedPath` to the `axum::extract` import. Updated `metrics_middleware` to extract `MatchedPath` from request extensions and use the route template as the `path` label value. Both the counter and histogram now carry `path`.
- `main.rs`: Removed the outer `.layer(middleware::from_fn(mw::metrics_middleware))` from the `app` builder. Added `.layer(middleware::from_fn(mw::metrics_middleware))` inside the `api` router block, between `inject_secrets` and `with_state(state)`.

## Files Modified

- `services/api-gateway/src/mw.rs` — added MatchedPath import, path extraction, path label to both metrics
- `services/api-gateway/src/main.rs` — moved metrics_middleware inside api router before with_state()

## Patch Fingerprint

fingerprint: cb832d320c89cdb2e15fb3890a4b41ed9fadacac1ebe07aeca98c78a0ca3f7f4
before_state: 24585d9354a0617e2eae9ca627199183c4fd88b056be3c2e8a907694b0bcb900
after_state: 8dfea4d56c8926a7b8239cb1369bd12672ca4161bb7583ed7e2d0ed9b85ad5b6

## Verification

command: cargo check -p api-gateway
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 13.04s — 11 pre-existing warnings, 0 errors

## Checklist Results

✅ io_http_requests_total counter includes a path label
✅ io_http_request_duration_seconds histogram includes a path label
✅ The path label uses MatchedPath route templates (e.g. /api/points/:id), not raw URI paths
✅ No cardinality explosion — MatchedPath always yields the route template, never concrete IDs
✅ metrics_middleware moved inside the api router (before with_state) so MatchedPath is available

## Cycle Check

NO COLLISION — no prior fingerprint matched (first attempt)

## Why This Attempt Succeeded

Both changes were straightforward. `MatchedPath` was already available from the axum crate already in use. Moving the middleware inside the router was a one-line change in `main.rs`. The build passes cleanly.

## Notes for Next Attempt

N/A
