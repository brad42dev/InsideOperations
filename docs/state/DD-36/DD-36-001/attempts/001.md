---
attempt: 1
task_id: DD-36-001
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:20:00Z
result: SUCCESS
---

## What Was Attempted

Added `UnixSocketCheck`, `RedisCheck` (stub), and `.non_critical()` builder methods to `crates/io-health/src/lib.rs`. Also added a 2-second internal timeout to `PgDatabaseCheck.check()` and a 1-second timeout to `UnixSocketCheck.check()` to prevent serial stacking under the handler's outer 5-second deadline.

Registered `PgDatabaseCheck` in all 9 services that have a `db` pool:
- api-gateway (via `state.db.clone()`)
- data-broker (via `db.clone()`)
- opc-service (via `db.clone()`)
- event-service (via `db.clone()`)
- archive-service (via `db.clone()`)
- auth-service (via `db.clone()`)
- email-service (via `state.db.clone()`)
- alert-service (via `state.db.clone()`)
- import-service (via `app_state.db.clone()`)

Registered `UnixSocketCheck(...).non_critical()` in data-broker and opc-service for the UDS path (`opc_broker_sock`).

recognition-service and parser-service have no db pools and no UDS dependencies — no checks registered there.

## Files Modified

- `crates/io-health/src/lib.rs` — added `UnixSocketCheck`, `RedisCheck`, `.non_critical()` builder on `PgDatabaseCheck` and `UnixSocketCheck`, internal timeouts on both check impls
- `services/api-gateway/src/main.rs` — `mut health`, registered `PgDatabaseCheck::new(state.db.clone())`
- `services/data-broker/src/main.rs` — `mut health`, registered `PgDatabaseCheck::new(db.clone())` and `UnixSocketCheck::new(cfg.opc_broker_sock.clone()).non_critical()`
- `services/opc-service/src/main.rs` — `mut health`, registered `PgDatabaseCheck::new(db.clone())` and `UnixSocketCheck::new(config.opc_broker_sock.clone()).non_critical()`
- `services/event-service/src/main.rs` — `mut health`, registered `PgDatabaseCheck::new(db.clone())`
- `services/archive-service/src/main.rs` — `mut health`, registered `PgDatabaseCheck::new(db.clone())`
- `services/auth-service/src/main.rs` — `mut health`, registered `PgDatabaseCheck::new(db.clone())`
- `services/email-service/src/main.rs` — `mut health`, registered `PgDatabaseCheck::new(state.db.clone())`
- `services/alert-service/src/main.rs` — `mut health`, registered `PgDatabaseCheck::new(state.db.clone())`
- `services/import-service/src/main.rs` — `mut health`, registered `PgDatabaseCheck::new(app_state.db.clone())`

## Patch Fingerprint

fingerprint: c1ffa63cf13e44eec4dac6bd3047a32fb12e331d5a6b6002ee2391bc1de0ddc4
before_state: (HEAD at start of task — no prior changes)
after_state: bd37520d48bdd74cd025988e82f0cfbd21bd289f0372c6fa31e928aa779ab092

## Verification

command: cargo check --workspace 2>&1 | tail -5
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 9.82s

## Checklist Results

✅ crates/io-health/src/lib.rs exports PgDatabaseCheck, UnixSocketCheck, RedisCheck stub
✅ services/api-gateway/src/main.rs calls health.register(PgDatabaseCheck::new(state.db.clone())) before health.into_router()
✅ services/data-broker/src/main.rs calls PgDatabaseCheck + UnixSocketCheck for opc_broker_sock
✅ Every service with a db pool (9 services) registers a PgDatabaseCheck
✅ Services with non-critical UDS deps (data-broker, opc-service) use .non_critical()
✅ GET /health/ready will return checks with at least one named entry (verified via code path)

## Cycle Check

NO COLLISION — no prior fingerprints existed.

## Why This Attempt Succeeded

All check types were added to io-health and registered in every service that has a db pool. The cargo check build passed cleanly with only pre-existing warnings. All verification checklist items confirmed by code inspection.

## Notes for Next Attempt

N/A
