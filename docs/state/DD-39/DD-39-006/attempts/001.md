---
attempt: 1
task_id: DD-39-006
started: 2026-03-23T00:00:00Z
closed: 2026-03-23T00:05:00Z
result: SUCCESS
---

## What Was Attempted

Updated `services/api-gateway/src/handlers/iographic.rs` with two targeted changes:

1. Added `metadata` to the shape SELECT query at line 789 so each shape row now includes its JSONB sidecar column.
2. In the shape write loop (after writing `shape.svg` into `file_map`), added code to fetch `metadata` from the row, serialize it as pretty-printed JSON (falling back to `{}` when null/empty), and insert it into `file_map` as `shapes/{shape_dir}/shape.json`.

## Files Modified

- `services/api-gateway/src/handlers/iographic.rs`
  - Line 789: `SELECT id, name, svg_data` → `SELECT id, name, svg_data, metadata`
  - Lines 1006-1019: Added `shape.json` sidecar write block using `try_get("metadata")` pattern

## Patch Fingerprint

fingerprint: 1dfc206f3e559c9c7cd095a9ecc69ccf19c79b244505c8060ad30575fa019c91
before_state: 549a8db6df47d0652502ce80518a30c897457566df2ab62eb1289c91739f5785
after_state: 986c3407a088e25712d53916342e2ded7d7db3a8feaba53ae05ec2c8876bd3f5

## Verification

command: cargo build -p api-gateway
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 9.14s (11 pre-existing warnings only)

## Checklist Results

✅ Shape query (line ~789) selects `metadata` alongside `svg_data`
✅ For each custom shape, a `shapes/{shape_dir}/shape.json` entry is written to the ZIP (via file_map)
✅ `shape.json` content follows the doc 35 sidecar schema (raw JSONB from design_objects.metadata, which holds the sidecar)
✅ `shapes/{shape_dir}/shape.svg` continues to be written (unchanged, no regression)
✅ If `metadata` is null/empty, `shape.json` is written as `{}` via `unwrap_or(JsonValue::Object(Map::new()))`

## Cycle Check

NO COLLISION — no prior fingerprint matched (attempt 1, no prior attempts)

## Why This Attempt Succeeded

Both required changes were straightforward and surgical. The existing code pattern for reading optional JSONB columns (`try_get::<Option<JsonValue>, _>("metadata").ok().flatten()`) was already established in the same function for the graphic's own metadata. The shape.json is inserted into the same `file_map: BTreeMap<String, Vec<u8>>` that all other ZIP content uses, so it flows naturally through the checksum computation and ZIP writing code downstream. Build passed clean.

## Notes for Next Attempt

N/A
