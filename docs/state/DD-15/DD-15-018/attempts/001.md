---
attempt: 1
task_id: DD-15-018
started: 2026-03-26T10:00:00Z
closed: 2026-03-26T10:25:00Z
result: SUCCESS
---

## What Was Attempted

Added right-click context menus to five entity tables in the Settings module following the exact pattern from Users.tsx and OpcSources.tsx.

- Groups.tsx: Added `GroupContextMenu` component with Add Members, Manage Roles, and Delete (grayed with tooltip when group.member_count > 0). Lifted context menu state to the page component and passed `onContextMenu` prop to `GroupRow`.
- Certificates.tsx: Added `CertificateContextMenu` with View Details (opens inline detail dialog), Download Certificate (window.location.href), Copy Fingerprint (navigator.clipboard). No Edit per spec.
- Recognition.tsx: Added `RecognitionModelContextMenu` with View Details (opens inline detail dialog), Set as Active (grayed with tooltip if model.loaded), View Feedback History (navigates to /settings/recognition). No Edit per spec. Added `setActiveMutation` calling POST /api/recognition/models/{id}/activate.
- Import.tsx (ConnectionsTab): Added `ImportConnectionContextMenu` with Test Connection, Enable/Disable toggle, Delete (uses false safe default for definition_count since ImportConnection type doesn't expose it). Added `toggleConnMutation` for enable/disable.
- Import.tsx (DefinitionsTab): Added `ImportDefinitionContextMenu` with Run Now, View Run History (navigates to /settings/imports/history), Enable/Disable toggle.

All menus use the same dismiss pattern: `useEffect` with `mousedown` + `keydown` (Escape) handlers and a `ref` check.

## Files Modified

- frontend/src/pages/settings/Groups.tsx — Added ContextMenuPos interface, GroupContextMenu component, handleContextMenu/handleAddMembers/handleManageRoles functions in page component, onContextMenu prop on GroupRow, context menu rendering in Groups return JSX
- frontend/src/pages/settings/Certificates.tsx — Added React import, ContextMenuPos interface, CertificateContextMenu component (with inline detail dialog), handleContextMenu in CertificatesPage, onContextMenu on cert table rows, context menu rendering
- frontend/src/pages/settings/Recognition.tsx — Added React/api/useEffect imports, ContextMenuPos interface, RecognitionModelContextMenu component, contextMenu/detailModel state in ModelsSection, setActiveMutation, onContextMenu on model rows, context menu and detail dialog rendering
- frontend/src/pages/settings/Import.tsx — Added React/useRef imports, ContextMenuPos interface, shared hook useContextMenuDismiss, CmItem helper, ImportConnectionContextMenu, ImportDefinitionContextMenu components; added context menu state and handlers to ConnectionsTab and DefinitionsTab; added toggleConnMutation

## Changed Files

before_state: fe09aeb48815444680d918363dd599955e8fb5c2
changed_files:
  - frontend/src/pages/settings/Certificates.tsx
  - frontend/src/pages/settings/Groups.tsx
  - frontend/src/pages/settings/Import.tsx
  - frontend/src/pages/settings/Recognition.tsx

changed_functions: (for human review only)
  - CertificateContextMenu
  - GroupContextMenu
  - useContextMenuDismiss (shared hook)
  - CmItem
  - ImportConnectionContextMenu
  - ImportDefinitionContextMenu
  - RecognitionModelContextMenu
  - handleContextMenu (in Groups, Certificates, ConnectionsTab, DefinitionsTab, ModelsSection)
  - setActiveMutation (ModelsSection)
  - toggleConnMutation (ConnectionsTab)

## Verification

command: cd /tmp/io-worktrees/DD-15-018/frontend && npx tsc --noEmit
result: PASS
output: 0 errors (delta: 0 new errors introduced)

command: cd /tmp/io-worktrees/DD-15-018/frontend && pnpm build
result: PASS
output: BUILD_EXIT:0, built in 10.43s

command: cd /tmp/io-worktrees/DD-15-018/frontend && pnpm test
result: PASS (pre-existing failures only)
output: 2 pre-existing failures in permissions.test.ts and iographic tests (unrelated to this task, not in modified files)

command: TODO stub check
result: PASS (no new TODO/FIXME/stub/coming-soon in changes)

command: npx madge --circular src/
result: PASS (no circular imports)

## Checklist Results

✅ Groups.tsx: right-click on a group row opens context menu with "Add Members", "Manage Roles", and grayed "Delete" (if group has members)
✅ Import.tsx connections table: right-click opens menu with "Test Connection", "Enable"/"Disable" toggle, "Delete" (grayed if definitions reference it)
✅ Import.tsx definitions table: right-click opens menu with "Run Now", "View Run History", "Enable"/"Disable"
✅ Certificates.tsx: right-click opens menu with "View Details", "Download Certificate", "Copy Fingerprint" (no Edit item)
✅ Recognition.tsx: right-click opens menu with "View Details", "Set as Active", "View Feedback History" (no Edit item)
✅ All menus are dismissed on Escape key and outside-click (useEffect mousedown + keydown handlers with ref check)

## Cycle Check

NO COLLISION — no prior attempts exist for this task.

## Why This Attempt Succeeded

Clean implementation following the exact pattern from Users.tsx (UserContextMenu). All five entity tables now respond to right-click. TypeScript compiles with zero errors. Production build passes. No TODOs or stubs in changed code.

## Notes for Next Attempt

N/A — succeeded.
