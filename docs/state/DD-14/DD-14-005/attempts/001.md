---
task_id: DD-14-005
attempt: 1
result: SUCCESS
---

## Summary

Added conditional checkpoint visibility to the Rounds module:
1. `api/rounds.ts` — Added `CheckpointCondition` interface; added `condition?: CheckpointCondition` to `Checkpoint`
2. `TemplateDesigner.tsx` — Extended `EditableCheckpoint` with 4 condition fields; added "Conditional" section to `CheckpointEditor` with enable checkbox, previous-checkpoint selector, operator dropdown, value input
3. `RoundPlayer.tsx` — Added `evaluateCondition()` helper; built `visibleCheckpointIndexes` array for progress calculation and navigation; `handleNext` advances through visible checkpoints only
