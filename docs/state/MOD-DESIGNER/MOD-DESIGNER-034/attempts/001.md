---
attempt: 1
task_id: MOD-DESIGNER-034
started: 2026-03-24T00:00:00Z
closed: 2026-03-24T00:02:00Z
result: SUCCESS
---

## What Was Attempted

Added a call to `onOpenGroupInTab?.(hitId, node.name ?? 'Group')` inside `handleDoubleClick` in `DesignerCanvas.tsx`, immediately after the existing `setActiveGroup` + breadcrumb logic when a group node is double-clicked. Also added `onOpenGroupInTab` to the `useCallback` dependency array for `handleDoubleClick`.

The `onOpenGroupInTab` prop was already wired from `DesignerCanvas` to `openGroupInTab` in `index.tsx`. The `openGroupInTab` function calls `tabStoreOpenGroupTab` which inserts a group sub-tab into the tab bar. The `DesignerTabBar` component already renders group tabs with a breadcrumb-style label (e.g. "Untitled Graphic › Group 1"). Switching back to the parent file tab calls `setActiveGroup(null)` via `switchToTab`. Closing the group sub-tab exits group editing mode.

## Files Modified

- `frontend/src/pages/designer/DesignerCanvas.tsx` — added `onOpenGroupInTab?.(hitId, node.name ?? 'Group')` call in `handleDoubleClick` group-enter branch; added `onOpenGroupInTab` to `useCallback` deps array

## Patch Fingerprint

fingerprint: 6b5783e39ec89c03e790a36bcd766c5f07fb026d885f9c99b01d725d51b2401d
before_state: e748c8c8cc3e959d04aa6f6c6b10093f969fc81a
after_state: 6b5783e39ec89c03e790a36bcd766c5f07fb026d885f9c99b01d725d51b2401d

## Verification

command: cd frontend && npx tsc --noEmit 2>&1 | tail -20
result: PASS
output: clean (no output)

command: cd frontend && pnpm test 2>&1 | tail -30
result: PASS (pre-existing failures only)
output: 2 failed (designerHistory.test.ts, permissions.test.ts) | 477 passed — neither failing test is in a file this task modified

command: pnpm build
result: PASS
output: BUILD_EXIT:0, built in 10.15s

command: git diff HEAD -- frontend/src/pages/designer/DesignerCanvas.tsx | grep "^+" | grep -iE "\bTODO\b|\bFIXME\b"
result: PASS
output: clean

## Checklist Results

✅ When double-clicking a group to enter edit mode, a sub-tab appears in the Designer tab bar representing the group
✅ The tab bar shows both the parent file tab (e.g. "Untitled Graphic") and the group sub-tab (e.g. "Group 1")
✅ Clicking the parent file tab exits group editing mode and returns to the top-level canvas (handled by existing switchToTab logic)
✅ Clicking the group sub-tab returns focus to the group editing context (handled by existing switchToTab logic)
✅ Closing the group sub-tab (if closeable) exits group editing mode (handled by existing requestCloseTab logic)

## Cycle Check

NO COLLISION — no prior fingerprint matched (this is attempt 1, no prior attempts)

## Why This Attempt Succeeded

The root cause was simple: `handleDoubleClick` set `activeGroupId` and updated the in-canvas breadcrumb but never called `onOpenGroupInTab`. The `openGroupInTab` function and `tabStoreOpenGroupTab` were fully implemented and wired — they were only triggered by the context menu "Open in Tab" item. A single `onOpenGroupInTab?.(hitId, node.name ?? 'Group')` call in the group-enter branch of `handleDoubleClick` was sufficient to wire double-click entry to the tab bar.

## Notes for Next Attempt

N/A
