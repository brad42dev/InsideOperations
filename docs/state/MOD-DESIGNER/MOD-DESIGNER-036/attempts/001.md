---
attempt: 1
task_id: MOD-DESIGNER-036
started: 2026-03-24T00:00:00Z
closed: 2026-03-24T00:03:00Z
result: SUCCESS
---

## What Was Attempted

Added a "View Alerts" `ContextMenuPrimitive.Item` to the display element point context menu section in `DesignerCanvas.tsx`. The item was inserted after the existing "Trend This Point" item and before the permission-gated "Investigate Point" item. It uses `disabled={!isBound}` (same pattern as the surrounding items) and navigates to `/forensics?tab=alarm&point={pointId}` when selected.

## Files Modified

- `frontend/src/pages/designer/DesignerCanvas.tsx` — Added "View Alerts" menu item (~line 5532) in the CX-POINT-CONTEXT block inside `DesignerContextMenuContent`, within the `displayElementNode` conditional branch. 10 lines added.

## Patch Fingerprint

fingerprint: e9af8c081d7718f0cc2718036eb0484469a089dda708509555b1a372aca095fd
before_state: 174bf9231811c185717ebe8e5b55efe1ec47b064
after_state: e9af8c081d7718f0cc2718036eb0484469a089dda708509555b1a372aca095fd

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit
result: PASS
output: clean (no output)

command: cd /home/io/io-dev/io/frontend && pnpm build
result: PASS
output: BUILD_EXIT:0, built in 9.88s

command: cd /home/io/io-dev/io/frontend && pnpm test
result: PASS (pre-existing failures only)
output: 2 pre-existing failures in permissions.test.ts (reports:generate, reports:schedule_manage not in type list) — unrelated to this task

command: git diff HEAD -- .../DesignerCanvas.tsx | grep "^+" | grep -iE "\bTODO\b|\bFIXME\b..."
result: PASS
output: clean (no new TODOs/FIXMEs)

## Checklist Results

✅ Right-clicking a display element shows "View Alerts" in context menu
✅ "View Alerts" is disabled when unbound (`disabled={!isBound}`)
✅ "View Alerts" is enabled when bound to a point (`isBound = !!pointId`)
✅ "Trend This Point" still present (line 5529)
✅ "Point Detail" still present (line 5518)
✅ "Bind Point..." still present
✅ "Change Type" still present
✅ "Investigate Point" and "Report on Point" not removed

## Cycle Check

NO COLLISION — no prior fingerprint matched (this is attempt 1, no prior fingerprints)

## Why This Attempt Succeeded

The fix was straightforward: a single `ContextMenuPrimitive.Item` block was missing from the display element point context menu section. Adding it after "Trend This Point" with `disabled={!isBound}` behavior and navigation to `/forensics?tab=alarm&point={pointId}` satisfies all acceptance criteria. TypeScript check and production build both pass clean.

## Notes for Next Attempt

N/A
