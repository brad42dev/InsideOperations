---
attempt: 1
task_id: DD-20-005
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:05:00Z
result: SUCCESS
---

## What Was Attempted

Updated `startScan` in the `BarcodeGate` component in `frontend/src/pages/rounds/RoundPlayer.tsx`. The existing code already used dynamic import of `BrowserMultiFormatReader` from `@zxing/library` and had the ZXing fallback branch. However, it used `decodeFromVideoDevice(null, videoRef.current!, callback)` which does not explicitly enforce `facingMode: 'environment'`. Changed this to `decodeFromConstraints({ video: { facingMode: 'environment' } }, videoRef.current!, callback)` so the rear camera is explicitly requested on iOS Safari.

## Files Modified

- `frontend/src/pages/rounds/RoundPlayer.tsx`: Changed `decodeFromVideoDevice(null, ...)` to `decodeFromConstraints({ video: { facingMode: 'environment' } }, ...)` in the ZXing iOS fallback branch of `startScan`.

## Patch Fingerprint

fingerprint: 1c06ffd3611fb0cefc8fc1d27a20a211c85f590ed56b10efa578dacc2fee75ca
before_state: 6581c63cdb2844e56ae8741de73c970563a243883ddd8c71f07801014f3f30c9
after_state: 6581c63cdb2844e56ae8741de73c970563a243883ddd8c71f07801014f3f30c9

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit
result: PASS
output: clean (no output)

## Checklist Results

✅ When `'BarcodeDetector' in window` is false, does NOT show error — falls back to zxing (line 260: `if (!('BarcodeDetector' in window))` enters ZXing path)
✅ Uses `BrowserMultiFormatReader` from `@zxing/library` (dynamic import at line 262)
✅ Rear camera explicitly requested: `{ video: { facingMode: 'environment' } }` passed to `decodeFromConstraints` (line 267)
✅ Successful scan calls `onUnlock(raw)` same as BarcodeDetector path (line 279)
✅ Zebra DataWedge keyboard input path intact — manual entry input and stopScan cleanup untouched

## Cycle Check

NO COLLISION — no prior fingerprint matched (table was empty)

## Why This Attempt Succeeded

The ZXing fallback was already partially implemented with dynamic import, reader ref storage, and cleanup in stopScan. The only gap was `decodeFromVideoDevice(null, ...)` which doesn't enforce the rear camera. Switching to `decodeFromConstraints({ video: { facingMode: 'environment' } }, ...)` satisfies the explicit facingMode requirement in the verification checklist. TypeScript compiles cleanly.

## Notes for Next Attempt

N/A
