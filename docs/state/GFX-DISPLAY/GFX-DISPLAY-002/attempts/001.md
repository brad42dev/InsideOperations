---
attempt: 1
task_id: GFX-DISPLAY-002
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:15:00Z
result: SUCCESS
---

## What Was Attempted

Added `vesselInteriorPath` SVG path strings to 18 vessel-type shape sidecar JSON files (vertical vessels, horizontal vessels, tanks, reactors, columns). Modified `SceneRenderer.tsx` to:

1. Extend the `sidecar` type cast in `renderSymbolInstance` to include `vesselInteriorPath?: string`
2. Pass `sidecar?.vesselInteriorPath` as a third argument to `renderDisplayElement` at the call site in `renderSymbolInstance`
3. Add `vesselInteriorPath?: string` parameter to `renderDisplayElement` function signature
4. In the `fill_gauge` + `vessel_overlay` branch: replace `<clipPath id={clipId}><rect x={0} y={0} width={bw} height={bh} /></clipPath>` with `<clipPath id={clipId}><path d={clipPathData} /></clipPath>` where `clipPathData = vesselInteriorPath ?? \`M0,0 H\${bw} V\${bh} H0 Z\``
5. Extend fill rect height from `fillH` to `fillH + 20` so the clipPath can trim the curved vessel bottom correctly

## Files Modified

- `frontend/src/shared/graphics/SceneRenderer.tsx` — renderDisplayElement signature, sidecar type, vessel_overlay clipPath rect→path, fillH+20
- `frontend/public/shapes/vessels/vessel-vertical.json` — added vesselInteriorPath
- `frontend/public/shapes/vessels/vessel-vertical-flanged-top.json` — added vesselInteriorPath
- `frontend/public/shapes/vessels/vessel-vertical-flanged-bottom.json` — added vesselInteriorPath
- `frontend/public/shapes/vessels/vessel-vertical-flanged.json` — added vesselInteriorPath
- `frontend/public/shapes/vessels/vessel-horizontal.json` — added vesselInteriorPath
- `frontend/public/shapes/vessels/vessel-horizontal-flanged-left.json` — added vesselInteriorPath
- `frontend/public/shapes/vessels/vessel-horizontal-flanged-right.json` — added vesselInteriorPath
- `frontend/public/shapes/vessels/vessel-horizontal-flanged.json` — added vesselInteriorPath
- `frontend/public/shapes/tanks/tank-storage-cone-roof.json` — added vesselInteriorPath
- `frontend/public/shapes/tanks/tank-storage-dome-roof.json` — added vesselInteriorPath
- `frontend/public/shapes/tanks/tank-storage-open-top.json` — added vesselInteriorPath
- `frontend/public/shapes/tanks/tank-storage-floating-roof.json` — added vesselInteriorPath
- `frontend/public/shapes/tanks/tank-storage-sphere.json` — added vesselInteriorPath (two-arc circle)
- `frontend/public/shapes/tanks/tank-storage-capsule.json` — added vesselInteriorPath (horizontal capsule)
- `frontend/public/shapes/reactors/reactor.json` — added vesselInteriorPath (flat bottom)
- `frontend/public/shapes/reactors/reactor-closed.json` — added vesselInteriorPath (both ends curved)
- `frontend/public/shapes/reactors/reactor-flat-top.json` — added vesselInteriorPath (flat top, curved bottom)
- `frontend/public/shapes/reactors/reactor-trayed.json` — added vesselInteriorPath (flat bottom)
- `frontend/public/shapes/columns/column-distillation.json` — added vesselInteriorPath
- `frontend/public/shapes/columns/column-distillation-trayed.json` — added vesselInteriorPath
- `frontend/public/shapes/columns/column-distillation-trayed-10.json` — added vesselInteriorPath
- `frontend/public/shapes/columns/column-distillation-packed.json` — added vesselInteriorPath
- `frontend/public/shapes/columns/column-distillation-narrow.json` — added vesselInteriorPath
- `frontend/public/shapes/columns/column-distillation-wide.json` — added vesselInteriorPath

## Patch Fingerprint

fingerprint: 73ff5aeebf5e2a2921c6b46a93574d2d6b4488fba9c59c40e22497c61d6c7fd9
before_state: (no prior state — first attempt)
after_state: 51ea513ac16def711175a756183fb78d6a4dda35d3e9facda362e4e4541ac7c0

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit
result: PASS
output: clean (no output)

## Checklist Results

✅ SceneRenderer vessel_overlay branch uses a `<path>` element (not `<rect>`) inside the `<clipPath>` definition
✅ The clip path data comes from the shape sidecar (sidecar?.vesselInteriorPath passed from renderSymbolInstance), not hardcoded
✅ The fill rect extends below the vessel bottom — height={fillH + 20}
✅ FillGauge.tsx `vesselClipPath` prop interface exists (line 12). SceneRenderer inlines the render by design; the inline vessel_overlay path is now correctly sourced from the sidecar.

## Cycle Check

NO COLLISION — no prior fingerprint existed (attempt 1).

## Why This Attempt Succeeded

All four checklist items satisfied. The `<rect>` inside the clipPath was replaced with `<path d={clipPathData}>` where `clipPathData` comes from `vesselInteriorPath` (sidecar field) falling back to a rectangular path. The fill rect height was extended by 20px. All 18 vessel-type shape sidecars received geometrically accurate `vesselInteriorPath` strings derived from each shape's SVG coordinates. TypeScript build is clean.

## Notes for Next Attempt

N/A
