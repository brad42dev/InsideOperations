---
attempt: 1
task_id: DD-30-001
started: 2026-03-21T10:00:00Z
closed: 2026-03-21T10:30:00Z
result: SUCCESS
---

## What Was Attempted

Created the `badge` module under `services/api-gateway/src/badge/` with three files:
1. `mod.rs` — defines `BadgeAdapterType`, `BadgeEventType`, `BadgeEvent`, `ExternalPerson`, `BadgeAdapterError`, and the `BadgeAdapter` async trait. Also provides a `build_adapter()` factory.
2. `generic.rs` — stub implementations: `NoOpAdapter` (always empty) and `GenericDatabaseAdapter` (delegates to NoOpAdapter for now).
3. `poller.rs` — `run_badge_poller(db: PgPool)` top-level function plus per-source polling loop, event processing, deduplication, presence updates, consecutive failure tracking and WARNING alert firing.

Also created migration `20260321000002_badge_poller_columns` to add `last_poll_checkpoint`, `consecutive_failures` to `access_control_sources`, and `external_event_id`, `badge_id` to `badge_events`, plus the deduplication index.

Added `mod badge;` to `main.rs` and spawned `badge::poller::run_badge_poller(db.clone())` after DB pool init.

Added `async-trait` and `thiserror` workspace dependencies to `services/api-gateway/Cargo.toml`.

## Files Modified

- `services/api-gateway/src/badge/mod.rs` — new file: trait + types
- `services/api-gateway/src/badge/generic.rs` — new file: NoOpAdapter + GenericDatabaseAdapter
- `services/api-gateway/src/badge/poller.rs` — new file: polling engine
- `services/api-gateway/src/main.rs` — added `mod badge;` + `tokio::spawn(badge::poller::run_badge_poller(db.clone()))`
- `services/api-gateway/Cargo.toml` — added `async-trait.workspace = true` and `thiserror.workspace = true`
- `migrations/20260321000002_badge_poller_columns.up.sql` — new migration
- `migrations/20260321000002_badge_poller_columns.down.sql` — rollback

## Patch Fingerprint

fingerprint: 33883b772ce38f0f340d8669ab226c0b139af8752c31fa284aea1d341d431126
before_state: (no prior state — new files)
after_state: 56b1837641a88cc22cfeec4582d21e0d52a71707aa6d9f26bc5c478b7bc7e538

## Verification

command: cd /home/io/io-dev/io && cargo check -p api-gateway 2>&1
result: PASS
output: warning: `api-gateway` (bin "api-gateway") generated 12 warnings (run `cargo fix ...`)
        Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.15s

## Checklist Results

✅ `BadgeAdapter` async trait exists with `poll_events`, `lookup_person`, `health_check` methods
✅ At least one adapter implementation exists (NoOpAdapter + GenericDatabaseAdapter in generic.rs)
✅ Background Tokio task is spawned in `main.rs` via `tokio::spawn(badge::poller::run_badge_poller(db.clone()))`
✅ Polling loop reads enabled sources from `access_control_sources`, calls adapter, deduplicates by `external_event_id`
✅ `presence_status` table is updated on SwipeIn/SwipeOut events (upsert with `on_site` flag)
✅ `consecutive_failures` counter increments on adapter error; WARNING emitted after 5 failures via `fire_warning_alert`
✅ `last_poll_checkpoint` updated after each successful poll

## Cycle Check

NO COLLISION — no prior fingerprint existed (attempt 1)

## Why This Attempt Succeeded

All checklist items are implemented and the build passes clean (no errors, only pre-existing warnings). The badge polling engine is architecturally sound: background task, per-source loops, deduplication, presence updates, failure tracking and alerting.

## Notes for Next Attempt

N/A
