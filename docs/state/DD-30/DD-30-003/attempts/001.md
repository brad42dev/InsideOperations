---
attempt: 1
task_id: DD-30-003
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:10:00Z
result: SUCCESS
---

## What Was Attempted

Added the `POST /api/presence/clear/:badge_id` endpoint to the api-gateway shifts handler, registered the route in the route builder, added `clearPresence` to the frontend shifts API client, and added a "Clear" button to the PresenceTab component for stale entries visible only to users with `presence:manage` permission.

Key adaptation: The `presence_status` table schema uses `user_id` (UUID) as primary key, not `badge_id` as a string. The actual columns are `on_site` (boolean), `stale_at`, and `updated_at` — no `is_on_site`, `badge_out_at`, or `status` column. The implementation was adapted to the actual schema while preserving the spec-required route URL and permission.

## Files Modified

- `services/api-gateway/src/handlers/shifts.rs` — Added `clear_presence` handler (after `get_presence`) and route `.route("/api/presence/clear/:badge_id", post(clear_presence))` in `shifts_routes()`
- `frontend/src/api/shifts.ts` — Added `clearPresence(badgeId: string)` method to `shiftsApi`
- `frontend/src/pages/shifts/index.tsx` — Added `useAuthStore` import; modified `PresenceTab` to add `useMutation` for clearPresence, permission check for `presence:manage`, and "Clear" button visible only on stale entries (`stale_at != null`)

## Patch Fingerprint
fingerprint: fd4954f005367a0af4ce67be599bf1fadb464986c089fffa2c42ab35c808ef81
before_state: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
after_state: f3dda481f115ace246dc2f8ff882825be3d19551d2e86f3ab5c93c2c81cd2df3

## Verification
command: cargo check -p api-gateway && cd frontend && npx tsc --noEmit
result: PASS
output: clean (11 pre-existing warnings, no errors)

## Checklist Results
✅ POST /api/presence/clear/:badge_id route exists in the route table (line 2270 of shifts.rs)
✅ Handler requires presence:manage permission (line 1281)
✅ Handler sets on_site = false, stale_at = NULL, updated_at = now() for specified badge_id (lines 1288-1290; adapted to actual schema columns)
✅ Handler returns 404 if badge_id not found in presence_status (lines 1300-1302)
✅ Response follows standard { "success": true, "data": ... } envelope via ok() helper (line 1299)

## Cycle Check
NO COLLISION — no prior fingerprint matched

## Why This Attempt Succeeded

All changes applied cleanly. Build passes with zero errors. All checklist items verified. The schema adaptation (using `on_site` instead of `is_on_site`, `user_id` instead of `badge_id`) was necessary because the actual `presence_status` table DDL differs from what the task spec assumed, but the functional intent is fully preserved.

## Notes for Next Attempt
N/A
