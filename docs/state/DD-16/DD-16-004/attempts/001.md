---
attempt: 1
task_id: DD-16-004
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:05:00Z
result: SUCCESS
---

## What Was Attempted

Added stale-recovery detection to the fanout engine:
1. Added `connections` parameter (`&DashMap<ClientId, mpsc::Sender<WsServerMessage>>`) to `fanout_batch` in `services/data-broker/src/fanout.rs`.
2. In `fanout_batch`, after calling `cache.update`, computed `was_stale = prev.as_ref().map(|p| p.stale).unwrap_or(false)` using the already-returned `Option<CachedValue>`.
3. If `was_stale` is true: construct `WsServerMessage::PointFresh { point_id, value, timestamp }` and send it directly to each subscribed client's channel (bypassing the accumulator), then `continue` to skip the deadband gate and accumulator path.
4. Updated callers in `uds.rs` (dispatch_frame) and `notify.rs` (run_notify_listener) to pass `connections`.
5. Added `#[cfg(test)] inner_insert_for_test` helper to `ShadowCache` in `cache.rs` to allow tests to seed stale entries.
6. Updated all existing `fanout_batch` call sites in the test module (added `connections` arg) and added two new tests: `fanout_sends_point_fresh_on_stale_recovery` and `fanout_no_point_fresh_on_normal_update_after_recovery`.

## Files Modified

- `services/data-broker/src/fanout.rs` — added `connections` param to `fanout_batch`; stale-recovery path sends `PointFresh` directly; two new tests added; all existing test calls updated
- `services/data-broker/src/cache.rs` — added `inner_insert_for_test` (#[cfg(test)]) helper
- `services/data-broker/src/uds.rs` — pass `connections` to `fanout_batch` in `dispatch_frame`
- `services/data-broker/src/notify.rs` — pass `connections` to `fanout_batch` in `run_notify_listener`

## Patch Fingerprint

fingerprint: 5c509237e28c2c5ede7134fc96070db787b2f063e39c3b7ff3fdfb7f59bb3dad
before_state: a896c668502a96a74dc3e6379d4b2724f5d2e46a2a50387e06ccba7dd0bd8fb6
after_state: e75d6aca098d185d53aee2fc7e4b2467d96528b48a734d963ecfe205d5ed1c3d

## Verification

command: cargo check -p data-broker
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.13s

## Checklist Results

✅ `ShadowCache::update` returns (via Option<CachedValue>.stale) whether the point was previously stale — checked in fanout.rs via `prev.as_ref().map(|p| p.stale).unwrap_or(false)`
✅ `fanout.rs` checks the stale flag before selecting message type: if previously stale → `WsServerMessage::PointFresh { point_id, value, timestamp }`, otherwise → normal accumulator path with `WsServerMessage::Update`
✅ `WsServerMessage::PointFresh` is constructed with `point_id`, `value`, `timestamp` fields per the pre-existing variant definition in io-bus; serialization uses the existing adjacently-tagged serde config
✅ `cache.mark_fresh` is called implicitly — `cache.update` unconditionally sets `stale: false` on insert, clearing the stale flag on every value update
✅ Stale flag is cleared when a new value arrives for a stale point (via `cache.update`'s `stale: false`); `mark_fresh` remains but its purpose is now served implicitly

## Cycle Check

NO COLLISION — no prior fingerprint existed (attempt 1)

## Why This Attempt Succeeded

The existing `cache.update` already returned `Option<CachedValue>` which included the `stale` field, so no signature change to cache.rs was needed for the stale detection. The `PointFresh` variant was already defined in io-bus; it only needed to be constructed in the fanout path. Adding `connections` to `fanout_batch` was the minimal change needed to send `PointFresh` directly (bypassing the accumulator). Build is clean with zero warnings.

## Notes for Next Attempt

N/A
