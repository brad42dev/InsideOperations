---
attempt: 1
task_id: DD-29-007
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:03:00Z
result: SUCCESS
---

## What Was Attempted

Fixed the `apply_group_role_mappings` function in `services/auth-service/src/handlers/oidc.rs`:

1. Verified which migration actually defines `idp_role_mappings`: migration 007 (`20260314000007_sites_categories.up.sql`) creates the table first with `provider_id` and `match_value` columns, `is_active` boolean, and match_type CHECK `('exact', 'prefix', 'regex')`. Migration 039 (`20260315000039_auth_providers.up.sql`) uses `CREATE TABLE IF NOT EXISTS` so it is a no-op.

2. Updated SQL query from `SELECT idp_group, role_id, match_type FROM idp_role_mappings WHERE provider_config_id = $1` to `SELECT match_value, role_id, match_type FROM idp_role_mappings WHERE provider_id = $1 AND is_active = true`.

3. Updated column read from `mapping.get("idp_group")` to `mapping.get("match_value")`.

4. Replaced `"contains" => g.contains(&idp_group)` match arm with `"regex" => regex::Regex::new(&match_value).map(|re| re.is_match(g)).unwrap_or(false)`.

5. Added `regex = "1"` to `services/auth-service/Cargo.toml`.

## Files Modified

- `services/auth-service/src/handlers/oidc.rs` — Fixed SQL query columns and FK, removed `contains` match type, added `regex` match type using regex crate
- `services/auth-service/Cargo.toml` — Added `regex = "1"` dependency

## Patch Fingerprint

fingerprint: 9143bbeb145dffe539f6ffef18900122ad4b170e499febb8a6ab2165141d7d26
before_state: 9bfc2ac88b5884e5f321e130a418395e9b282ebb765d7c26b11a2f07866b8def
after_state: b481405702fd21c084c588bcd71352e05622c7723e0a6ed2b946066256e4afd6

## Verification

command: cargo check -p auth-service 2>&1 | tail -20
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 4.88s (2 pre-existing unused import warnings only)

## Checklist Results

✅ SQL query FK column uses `provider_id` (actual DB migration 007 column)
✅ SQL query data column uses `match_value` (actual DB migration 007 column — spec says `idp_group_value` but actual migration has `match_value`; aligned with actual DB)
✅ `is_active = true` filter added to query (migration 007 uses `is_active`, not `enabled`)
✅ `mapping.get("match_value")` used for column read
✅ `regex` match type implemented using `regex::Regex::new`
✅ `contains` match type removed (not in spec; replaced by `regex`)
✅ `regex = "1"` added to auth-service Cargo.toml
✅ LDAP caller (`ldap_auth.rs:302`) still works — function signature unchanged, takes same Uuid parameter
✅ SAML caller (`saml.rs:394`) still works — function signature unchanged
✅ Build check: PASS

## Cycle Check

NO COLLISION — no prior fingerprint matched (first attempt)

## Why This Attempt Succeeded

All required changes were made correctly. Key insight: migration 007 runs before 039, and 039 uses `IF NOT EXISTS` so is a no-op. The actual DB schema has `match_value` (not `idp_group_value`) and `is_active` (not `enabled`). The code was updated to match the actual schema. The `regex` crate was added and the match arm implemented correctly.

## Notes for Next Attempt

N/A
