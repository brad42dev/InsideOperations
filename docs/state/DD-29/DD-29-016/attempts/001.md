---
attempt: 1
task_id: DD-29-016
started: 2026-03-24T10:00:00Z
closed: 2026-03-24T10:15:00Z
result: SUCCESS
---

## What Was Attempted

Added `useUiStore.setLockMeta({ hasPin: true/false })` calls in `UserProfile.tsx`'s `PinSetupSection` component — specifically after successful `authApi.setPin()` and `authApi.deletePin()` calls. Also added the `useUiStore` import.

The `bootSyncLock` in `AppShell.tsx` correctly initializes `lockMeta.hasPin` from the server on page load. However, when the user sets or removes a PIN during a session (without reloading), `lockMeta.hasPin` was never updated. The Lock Screen button calls `lock(undefined, true)` which merges `undefined` into the existing lockMeta — preserving the stale `hasPin: false`. The LockOverlay component already had correct logic to show PIN mode when `hasPin=true`, it just never received the updated value.

## Files Modified

- `frontend/src/pages/profile/UserProfile.tsx`: added `useUiStore` import; added `useUiStore.getState().setLockMeta({ hasPin: true })` after successful setPin; added `useUiStore.getState().setLockMeta({ hasPin: false })` after successful deletePin.

## Patch Fingerprint

fingerprint: 9c9a8247e1ae18df825f075b25e4650f3a0a65cd45c16a812df6d59cbeae7a18
before_state: ba0c78b796d6d31317edd2036fbe3e1242268fce
after_state: 9c9a8247e1ae18df825f075b25e4650f3a0a65cd45c16a812df6d59cbeae7a18

## Verification

command: cd frontend && npx tsc --noEmit
result: PASS
output: clean

command: cd frontend && pnpm build
result: PASS
output: BUILD_EXIT:0

command: cd frontend && pnpm test
result: PASS (with pre-existing warnings)
output: 2 pre-existing failures in permissions.test.ts (reports:generate, reports:schedule_manage permissions) — unrelated to this task. 477 tests pass.

## Checklist Results

✅ When a user has a PIN set, locking the screen shows PIN entry (field or tab) alongside or instead of password — lockMeta.hasPin is now updated to true immediately after successful setPin
✅ Entering the correct 6-digit PIN on the lock screen dismisses the dialog and resumes the session — handlePinSubmit in LockOverlay calls authApi.verifyPin, on success calls handleUnlockSuccess which fades out overlay and calls unlock()
✅ When no PIN is set, the lock screen shows password-only — hasPin=false → unlockMode='password' (unchanged existing behavior)
✅ The PIN verify path calls POST /api/auth/pin/verify (or equivalent) — authApi.verifyPin calls POST /api/auth/verify-pin (not verify-password)

## Cycle Check

NO COLLISION — no prior fingerprint matched (no prior attempts)

## Why This Attempt Succeeded

The root cause was a missing state sync: `UserProfile.tsx` successfully called the API to set/delete a PIN but never updated `useUiStore.lockMeta.hasPin`. `LockOverlay.tsx` already had complete correct logic for PIN-based unlock — it just never received `hasPin=true`. The fix is minimal: 3 new lines in `UserProfile.tsx` (1 import + 2 `setLockMeta` calls).

## Notes for Next Attempt

N/A
