---
attempt: 1
task_id: DD-29-015
started: 2026-03-24T10:00:00Z
closed: 2026-03-24T10:12:00Z
result: SUCCESS
---

## What Was Attempted

Added the two missing route registrations in the API gateway (`services/api-gateway/src/main.rs`) that were preventing the frontend from reaching the PIN management endpoints. The auth-service handler (`services/auth-service/src/handlers/pin.rs`), DB migration (`migrations/20260321000004_pin_management.up.sql`), and frontend code were all fully implemented — only the gateway routing was missing.

Specifically, added:
- `POST /api/auth/pin` and `DELETE /api/auth/pin` — proxied to auth-service, mapped to `set_pin` and `delete_pin` handlers
- `POST /api/auth/verify-pin` — proxied to auth-service, mapped to `verify_pin` handler

These were inserted in the "Session lock / lock-screen unlock" comment block alongside `/api/auth/verify-password`.

## Files Modified

- `services/api-gateway/src/main.rs`: Added 3 new route registrations for PIN endpoints (lines 188–190 after the edit):
  ```
  .route("/api/auth/pin", post(proxy_auth).delete(proxy_auth))
  .route("/api/auth/verify-pin", post(proxy_auth))
  ```

## Patch Fingerprint

fingerprint: 442ff0637b2478560b9d696c0eecda36db8423085631d2c908824be29c054ecf
before_state: 24c680799ac598d3da542f5fb4f6cf9e02e439df
after_state: 442ff0637b2478560b9d696c0eecda36db8423085631d2c908824be29c054ecf

## Verification

command: cargo check -p api-gateway 2>&1 | tail -10
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.20s (11 pre-existing warnings, no errors)

command: grep -n "api/auth/pin\|api/auth/verify-pin" services/api-gateway/src/main.rs
result: PASS
output:
  188: .route("/api/auth/pin", post(proxy_auth).delete(proxy_auth))
  190: .route("/api/auth/verify-pin", post(proxy_auth))

command: grep -n "/auth/pin\|/auth/verify-pin" services/auth-service/src/main.rs
result: PASS
output:
  86: .route("/auth/pin", post(handlers::pin::set_pin).delete(handlers::pin::delete_pin))
  87: .route("/auth/verify-pin", post(handlers::pin::verify_pin))

## Checklist Results

✅ POST /api/auth/pin accepts 6-digit PIN + current password — gateway routes to auth-service set_pin which validates format, verifies password, stores Argon2 hash
✅ DELETE /api/auth/pin removes the PIN (requires current password) — gateway routes to auth-service delete_pin
✅ Lock screen shows PIN entry option once PIN is set — LockOverlay reads hasPin from lockMeta and sets unlockMode to 'pin' when hasPin is true
✅ Lock screen allows unlock via PIN OR password — LockOverlay implements both modes with canSwitchToPin/canSwitchToPassword logic
✅ "Remove PIN" button on profile page works — UserProfile calls authApi.deletePin, gateway now routes DELETE /api/auth/pin
✅ No 404 on /api/auth/pin — routes registered in gateway

## Cycle Check

NO COLLISION — no prior fingerprint matched (first attempt)

## Why This Attempt Succeeded

The root cause was a missing route registration in the API gateway. The auth-service had fully implemented `set_pin`, `delete_pin`, and `verify_pin` handlers, the DB migration added the `lock_pin_hash` column, and the frontend had working forms and API calls — but the gateway's explicit route list did not include `/api/auth/pin` or `/api/auth/verify-pin`, causing all requests to fall through to 404. Adding the two `.route(...)` calls resolved the issue immediately.

## Notes for Next Attempt

N/A
