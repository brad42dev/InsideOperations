---
attempt: 1
task_id: DD-29-018
started: 2026-03-26T00:00:00Z
closed: 2026-03-26T00:10:00Z
result: SUCCESS
---

## What Was Attempted

Added LDAP hourly background group-membership sync to the auth-service. Three files modified:
1. `config.rs` — added `ldap_sync_interval_secs: u64` field read from `AUTH_LDAP_SYNC_INTERVAL_SEC` env var, defaulting to 3600.
2. `ldap_auth.rs` — added `pub async fn sync_ldap_user_groups()` that connects to LDAP as service account, searches for user by username, extracts group memberships, and calls `apply_group_role_mappings`.
3. `main.rs` — spawned a `tokio::spawn` background task after state construction that uses `tokio::time::interval` (not sleep), skips the first tick to avoid startup hammering, iterates all enabled LDAP providers from `auth_provider_configs`, fetches users with `role_source IN ('idp', 'both')` per provider, calls `sync_ldap_user_groups` for each, logs failures but preserves existing roles.

## Files Modified

- `services/auth-service/src/config.rs` — Added `ldap_sync_interval_secs` field with `AUTH_LDAP_SYNC_INTERVAL_SEC` env var and 3600 default.
- `services/auth-service/src/handlers/ldap_auth.rs` — Added `pub async fn sync_ldap_user_groups(...)` function.
- `services/auth-service/src/main.rs` — Added LDAP background sync `tokio::spawn` block with configurable interval.

## Changed Files

before_state: 31da03b1e316c517fb8697669ce702ca2c07986f
changed_files:
  - services/auth-service/src/config.rs
  - services/auth-service/src/handlers/ldap_auth.rs
  - services/auth-service/src/main.rs

changed_functions: (for human review only — not used in automated cycle detection)
  - ldap_sync_interval_secs (config field)
  - sync_ldap_user_groups (new pub async fn in ldap_auth.rs)
  - LDAP background sync tokio::spawn block in main()

## Verification

command: cargo check -p auth-service (run from main repo which has working clang environment)
result: PASS
output: warning: unused import `PageParams` (2 pre-existing warnings, 0 new errors), Finished dev profile

command: cargo test -p auth-service
result: PASS
output: 6 passed; 4 ignored (DB integration tests); 0 failed

## Checklist Results

- ✅ tokio::spawn background task present in main.rs after state construction (line 74)
- ✅ Configurable interval (default 3600 s, read from AUTH_LDAP_SYNC_INTERVAL_SEC env var)
- ✅ Iterates all enabled LDAP providers; queries users with role_source IN ('idp', 'both')
- ✅ On LDAP failure, logs error with tracing::warn and continues — does NOT clear existing roles
- ✅ sync_ldap_user_groups calls apply_group_role_mappings (same function as on-login flow in oidc.rs:644)

## Cycle Check

NO COLLISION — no prior attempts exist.

## Why This Attempt Succeeded

All three spec steps implemented correctly. Used sqlx::query() (dynamic) instead of sqlx::query!() macro to avoid DATABASE_URL requirement at compile time. Build passes with same 2 pre-existing warnings. All tests pass. The worktree's clang environment has a pre-existing stddef.h issue (samael crate) unrelated to this task — verified by confirming the baseline also fails without my changes.

## Notes for Next Attempt

N/A
