---
attempt: 1
task_id: OPC-BACKEND-002
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:15:00Z
result: SUCCESS
---

## What Was Attempted

Added A&C operator method HTTP endpoints to the opc-service. This involved four files:

1. `services/opc-service/src/state.rs` — Added `SessionRegistry` type alias (`Arc<std::sync::Mutex<HashMap<Uuid, Arc<RwLock<Session>>>>>`) and `sessions: SessionRegistry` field to `AppState`.

2. `services/opc-service/src/driver.rs` — Added `sessions: SessionRegistry` parameter to `run_source` and `run_source_once`. Inserted session into registry immediately after connection is established (before cert registration). Removed from registry before the CloseSession disconnect call. Added `pub fn call_alarm_method(session: &Session, condition_node_id: &str, method_name: &str, args: Option<Vec<Variant>>) -> StatusCode` helper that constructs ns=1 string NodeIds and calls `session.call(...)`.

3. `services/opc-service/src/main.rs` — Created `sessions: SessionRegistry` in `main()`, passed it to `AppState` and to `driver::run_source`. Added four routes: POST `/internal/alarm/:source_id/acknowledge`, `/enable`, `/disable`, `/shelve`. Implemented handlers: each clones the Arc from the registry under the Mutex lock then calls `driver::call_alarm_method` inside `tokio::task::spawn_blocking`. Returns 404 if source not in registry, 204 on OPC Good, 422 with JSON on OPC non-Good.

4. `services/opc-service/Cargo.toml` — Added `hex.workspace = true` dependency (for EventId hex decoding in acknowledge handler).

## Files Modified

- `services/opc-service/src/state.rs` — Added SessionRegistry type and sessions field
- `services/opc-service/src/driver.rs` — Added sessions param to run_source/run_source_once, session registration, call_alarm_method helper
- `services/opc-service/src/main.rs` — Session registry creation, 4 alarm routes, 4 handlers
- `services/opc-service/Cargo.toml` — Added hex dependency

## Patch Fingerprint
fingerprint: 3ef2f7146521274ef33be546c43c1d46903d8266ef2bb6f5838c6d9fe74ea869
before_state: 365e2a32de02ccd30351609f5f9372e2926618378274c4d827f6d4db8d0ff694
after_state: 2e3d49b8f5437c864bdbcd783b7863fd48dc3db9aaa9eb6ac7e7cb0f1267c3b9

## Verification
command: cargo build -p opc-service 2>&1
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.13s (4 pre-existing warnings only, no new warnings)

## Checklist Results
✅ AppState carries Arc<Mutex<HashMap<Uuid, Arc<RwLock<Session>>>>> via sessions: SessionRegistry field
✅ POST /internal/alarm/:source_id/acknowledge exists and calls driver::call_alarm_method with event_id and comment args
✅ POST /internal/alarm/:source_id/shelve with type field (timed/oneshot/unshelve) and optional shelving_time_ms exists
✅ POST /internal/alarm/:source_id/enable and /disable exist
✅ Method NodeIds use ns=1 string format: NodeId::new(1u16, UAString::from("alarm-method:..."))
✅ 204 on Good OPC status, 404 if source not in registry, 422 with JSON body if non-Good OPC status

## Cycle Check
NO COLLISION — no prior fingerprint existed (first attempt)

## Why This Attempt Succeeded

All changes compiled cleanly. The implementation followed the task spec exactly:
- SessionRegistry uses std::sync::Mutex (not async), cloned under lock before spawn_blocking
- call_alarm_method uses ns=1 string NodeIds per spec
- Four HTTP endpoints cover all six A&C methods (Enable, Disable, Acknowledge, TimedShelve, OneShotShelve, Unshelve) through the shelve endpoint's type field
- Correct HTTP status codes (204/404/422)

## Notes for Next Attempt
N/A
