---
attempt: 1
task_id: DD-12-004
started: 2026-03-23T10:00:00Z
closed: 2026-03-23T10:20:00Z
result: SUCCESS
---

## What Was Attempted

Added drag-to-reorder for investigation stages in `InvestigationWorkspace.tsx` using `@dnd-kit/core` and `@dnd-kit/sortable` (already present as project dependencies).

Changes:
1. Added imports for `DndContext`, `PointerSensor`, `useSensor`, `useSensors`, `DragEndEvent` from `@dnd-kit/core`, and `SortableContext`, `useSortable`, `verticalListSortingStrategy`, `arrayMove` from `@dnd-kit/sortable`, and `CSS` from `@dnd-kit/utilities`.
2. Added `useEffect` to existing React import.
3. Added `dragHandleProps?: React.HTMLAttributes<HTMLSpanElement>` prop to `StageCard`, with a `⠿` grip icon rendered in the stage header when `!readOnly && dragHandleProps`.
4. Added a new `SortableStageCard` component wrapping `StageCard` with `useSortable` (disabled when `isReadOnly`).
5. Added `localStageOrder` state + `useEffect` to reset it on investigation ID change + `sensors` for PointerSensor with 5px activation distance.
6. Added `handleDragEnd` function that: computes new positions with `arrayMove`, sets optimistic local order, then calls `forensicsApi.updateStage` for each stage whose `sort_order` changed.
7. Wrapped the stage list render in `<DndContext sensors={sensors} onDragEnd={handleDragEnd}>` + `<SortableContext ...>`, replacing `StageCard` with `SortableStageCard`.

## Files Modified

- `frontend/src/pages/forensics/InvestigationWorkspace.tsx` — Added dnd-kit imports, drag handle prop to StageCard, SortableStageCard component, sensors/handleDragEnd, and DndContext+SortableContext wrapping the stage list.

## Patch Fingerprint

fingerprint: 0766d70b0d86c015268307ed8c2ce818116ecb714e9887cab6843e436d8af1d1
before_state: 3b56048776d9bcd31c8d6acda1542a10bf3ba830
after_state: 0766d70b0d86c015268307ed8c2ce818116ecb714e9887cab6843e436d8af1d1

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit 2>&1 | tail -20
result: PASS
output: clean (no output)

command: rm -f /tmp/io-build.log && cd frontend && pnpm build > /tmp/io-build.log 2>&1; echo "BUILD_EXIT:$?"
result: PASS
output: BUILD_EXIT:0, built in 9.37s

command: cd /home/io/io-dev/io/frontend && pnpm test 2>&1 | tail -20
result: PASS (pre-existing failures only)
output: 2 failed in permissions.test.ts (pre-existing unrelated failures — permissions count mismatch), 471 passed

## Checklist Results

✅ Each StageCard has a visible drag handle (⠿ grip icon in stage header, visible only when !readOnly && dragHandleProps provided)
✅ Dragging a stage and dropping it at a new position updates visual order immediately (setLocalStageOrder optimistic update)
✅ After drop, forensicsApi.updateStage is called with the new sort_order for all reordered stages (handleDragEnd loop)
✅ Drag-to-reorder is disabled when the investigation is read-only (disabled: readOnly in useSortable, dragHandleProps hidden)
✅ Stage order is stable on page reload (sort_order persisted to server via API calls, server sorts by sort_order)

## Cycle Check

NO COLLISION — no prior fingerprint matched (first attempt)

## Why This Attempt Succeeded

All checklist items pass. The implementation follows the exact same pattern as PlaylistManager.tsx which already uses @dnd-kit in this codebase. TypeScript type check is clean, production build succeeds.

## Notes for Next Attempt

N/A
