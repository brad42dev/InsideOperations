---
attempt: 1
task_id: DD-12-008
started: 2026-03-22T10:00:00Z
closed: 2026-03-22T10:15:00Z
result: SUCCESS
---

## What Was Attempted

Updated `frontend/src/pages/forensics/index.tsx` to add a Radix UI `ContextMenu` wrapper around each `InvestigationCard` in the Forensics investigation list.

- Added `import * as ContextMenu from '@radix-ui/react-context-menu'` (already in package.json as `^2.2.16`)
- Added `import { usePermission } from '../../shared/hooks/usePermission'`
- Added context menu style constants: `menuContentStyle`, `menuItemStyle`, `menuItemDestructiveStyle`, `separatorStyle`
- Updated `InvestigationCard` props to include: `onClose`, `onCancel`, `onExport`, `onShare`, `onDelete`
- Wrapped card `<div>` in `<ContextMenu.Root><ContextMenu.Trigger asChild>` with `<ContextMenu.Portal><ContextMenu.Content>` containing all required menu items
- Used `usePermission('forensics:export')` and `usePermission('forensics:share')` — items hidden (not disabled) when lacking
- Close/Cancel shown only when `inv.status === 'active'`
- Delete shown only when `inv.status === 'active'`, styled with destructive color
- Added `closeMutation`, `cancelMutation`, `deleteMutation` in `ForensicsPage` using `forensicsApi` methods with `queryClient.invalidateQueries` on success
- Added `handleExport` (calls `forensicsApi.exportInvestigation` and opens URL) and `handleShare` (navigates to share action route)
- Passed all handlers to `InvestigationCard` at the call site

## Files Modified

- `frontend/src/pages/forensics/index.tsx` — Added Radix ContextMenu to InvestigationCard, mutations in ForensicsPage

## Patch Fingerprint

fingerprint: 638d7ed9530c328d4c30c1f51b890695666b9044a52abf9d822b9923d7e652de
before_state: 6cd5409a56df24a9c827d7e720ca5cb24af4af8cb963cd2fae5219888ae626e6
after_state: 04b442358ab24fc7345f42c4d7f3bfebe05021309651b7ce0f744583618d4e6a

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit
result: PASS
output: clean (no output)

## Checklist Results

✅ Right-clicking an InvestigationCard opens a Radix UI ContextMenu (not a custom div) — ContextMenu.Root + ContextMenu.Trigger asChild wrapping the card div
✅ Menu includes: Open, Close (when status=active), Cancel (when status=active), Export, Share, Delete — all present with proper conditional rendering
✅ Destructive actions (Delete, Cancel) are separated from navigational actions by a separator — separatorStyle divs before each group
✅ Delete is only shown when investigation is active — guarded by `inv.status === 'active'`
✅ Permission-lacking items are hidden (not disabled) — canExport and canShare gate rendering entirely

## Cycle Check

NO COLLISION — no prior fingerprint matched (this is attempt 1, no prior fingerprints)

## Why This Attempt Succeeded

All checklist items satisfied. TypeScript build passes cleanly. Used Radix ContextMenu primitives as required by spec. Permission checks use `usePermission` hook from shared hooks. Mutations call the correct `forensicsApi` methods and invalidate the investigations query on success.

## Notes for Next Attempt

N/A
