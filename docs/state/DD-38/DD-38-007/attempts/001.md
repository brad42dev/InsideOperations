---
attempt: 1
task_id: DD-38-007
started: 2026-03-23T00:00:00Z
closed: 2026-03-23T00:10:00Z
result: SUCCESS
---

## What Was Attempted

Replaced the hardcoded `<Navigate to={isAuthenticated ? '/settings/users' : '/login'} replace />` on the index route in `frontend/src/App.tsx` with a permission-aware `DefaultRedirect` component.

Added:
- A `useDefaultRoute()` hook that iterates `ROUTE_REGISTRY` (imported from `frontend/src/shared/routes/registry.ts`, which was already implemented as DD-38-001) and returns the path of the first entry whose `permission` is present in `user.permissions`. Returns `/login` if unauthenticated, `/settings` as final fallback.
- A `DefaultRedirect` component that calls `useDefaultRoute()` and renders `<Navigate to={route} replace />`.
- An import for `ROUTE_REGISTRY` from `./shared/routes/registry`.
- Removed the now-unused `const { isAuthenticated } = useAuthStore()` from `AppRoutes`.

## Files Modified

- `frontend/src/App.tsx`: Added `ROUTE_REGISTRY` import, `useDefaultRoute` hook, `DefaultRedirect` component; replaced index route navigate with `<DefaultRedirect />`; removed unused `isAuthenticated` from `AppRoutes`.

## Patch Fingerprint

fingerprint: e35dd1c174651c502519fbd7788b80b17299ab834251c8ff19d0391b3bdfb619
before_state: (no stash — clean diff against HEAD)
after_state: e7339a78c13b178eff389b67465727d9be784f8f71601ff026f05ff49f005e0d

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit 2>&1 | tail -20
result: PASS
output: clean (no output)

## Checklist Results

✅ The index route does NOT unconditionally redirect to /settings/users
✅ The default redirect resolves to the first route whose permission is in user.permissions (in sidebar order via ROUTE_REGISTRY.find)
✅ A user with only console:read lands on /console (ROUTE_REGISTRY[0] matches)
✅ A user with only log:read lands on /log (console/process/dashboards/reports/forensics skipped)
✅ An unauthenticated user is redirected to /login (useDefaultRoute returns '/login' when !isAuthenticated)

## Cycle Check

NO COLLISION — no prior fingerprint matched (table was empty)

## Why This Attempt Succeeded

The fix was straightforward: import the already-implemented ROUTE_REGISTRY, add a useDefaultRoute hook that does an ordered permission lookup, wrap it in a DefaultRedirect component, and replace the hardcoded Navigate. TypeScript passes cleanly.

## Notes for Next Attempt

N/A
