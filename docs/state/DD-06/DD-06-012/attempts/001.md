---
attempt: 1
task_id: DD-06-012
started: 2026-03-22T10:00:00Z
closed: 2026-03-22T10:15:00Z
result: SUCCESS
---

## What Was Attempted

Created the popup detection utility, the banner component, and wired both into
AppShell. Three files created/modified:

1. `frontend/src/shared/utils/popupDetection.ts` — new file exporting `detectPopupBlocked()` with
   the exact probe from the spec (window.open probe, closes on success, returns true if blocked).

2. `frontend/src/shared/components/PopupBlockedBanner.tsx` — new file with:
   - `usePopupBlockedState()` hook: runs probe at mount, manages isBlocked/isDismissed state,
     sessionStorage persistence for dismissed state, checkAgain/dismiss/restore actions.
   - `PopupBlockedBanner` component: full-width warning banner using `--io-warning` token,
     browser-specific instructions (no chrome:// or about: links), Check again and Dismiss buttons.
   - `PopupBlockedIndicator` component: compact warning button for top bar after dismiss.

3. `frontend/src/shared/layout/AppShell.tsx` — modified:
   - Imported PopupBlockedBanner, PopupBlockedIndicator, usePopupBlockedState.
   - Added `const popupBlockedState = usePopupBlockedState()` after other state declarations.
   - Added `<PopupBlockedIndicator state={popupBlockedState} />` in top bar right section, gated !isKiosk.
   - Added `<PopupBlockedBanner state={popupBlockedState} />` below `</header>`, above `<main>`, gated !isKiosk.

LockOverlay.tsx was NOT modified — it already correctly handles the SSO popup blocked case inline
(sets ssoPopupBlocked=true when window.open returns null, shows inline notice). The detection
there is implicit (actual window.open call result), not the pre-flight probe.

## Files Modified

- `frontend/src/shared/utils/popupDetection.ts` — created new
- `frontend/src/shared/components/PopupBlockedBanner.tsx` — created new
- `frontend/src/shared/layout/AppShell.tsx` — added imports, hook call, indicator in topbar, banner below header

## Patch Fingerprint
fingerprint: 2e62077f4fc25591fb9288e525067f2f728fa3140071c87ba277e67ad9118326
before_state: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
after_state: 3d77758dd04eaf5726ff0af28a9a64c484cfa3e0b36935c2b1b2bbe735e959c8

## Verification
command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit 2>&1
result: PASS
output: clean (no output)

## Checklist Results
✅ PopupBlockedBanner.tsx component exists
✅ Detection function correctly identifies blocked popups (uses spec-prescribed probe)
✅ "Check again" clears the banner when popups are re-enabled (checkAgain() re-probes)
✅ Dismiss → indicator visible → click indicator → banner restores (dismiss/restore/sessionStorage)
✅ AppShell renders banner below TopBar (line 1509, after </header>, before <main>)
✅ LockOverlay handles null window.open() gracefully (already handled at lines 514-518)

## Cycle Check
NO COLLISION — no prior fingerprint existed for this task.

## Why This Attempt Succeeded

All requirements satisfied in a single pass. The utility function, banner component, and AppShell
wiring were implemented cleanly with no TypeScript errors. The LockOverlay already had correct
inline handling and did not need modification for the core acceptance criteria.

## Notes for Next Attempt
N/A
