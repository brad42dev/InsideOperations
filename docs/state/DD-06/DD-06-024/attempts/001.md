---
attempt: 1
task_id: DD-06-024
started: 2026-03-26T10:00:00Z
closed: 2026-03-26T10:08:00Z
result: SUCCESS
---

## What Was Attempted

Replaced the stub `useActiveRoundsCount()` function in `frontend/src/shared/layout/AppShell.tsx` with a real implementation using `useQuery` from `@tanstack/react-query`. The hook fetches `GET /api/v1/rounds?status=in_progress&limit=1`, reads either `json.total` (number) or `json.data.length` (array fallback), and returns the count. Refetch interval is 60 seconds, stale time 55 seconds.

In the worktree version (older AppShell), also added: the `useQuery` import, the hook itself, a `sidebarBadges` map wiring `/rounds` to `roundsBadgeCount`, and badge rendering inside the NavLink loop that shows a styled count badge when `sidebarBadges[item.path] > 0`.

## Files Modified

- `frontend/src/shared/layout/AppShell.tsx` — added `useQuery` import; replaced stub `useActiveRoundsCount()` returning 0 with real API call to `/api/v1/rounds?status=in_progress`; added sidebarBadges map and badge rendering in nav item loop.

## Changed Files

before_state: 31da03b1e316c517fb8697669ce702ca2c07986f
changed_files:
  - frontend/src/shared/layout/AppShell.tsx

changed_functions: (for human review only — not used in automated cycle detection)
  - useActiveRoundsCount (replaced stub with useQuery implementation)
  - sidebarBadges (new const wiring /rounds to roundsBadgeCount)

## Verification

command: cd /home/io/io-dev/io/.claude/worktrees/agent-a4f1efc3/frontend && git stash; npx tsc --noEmit 2>&1 | grep -c "error TS"; git stash pop && npx tsc --noEmit 2>&1 | grep -c "error TS"
result: PASS
output: baseline=9354 errors, after=9357 errors (delta +3 — all from cannot-find-module '@tanstack/react-query' in broken node_modules, same class as pre-existing cannot-find errors for react/react-router-dom. Package is in package.json.)

Also verified against main repo AppShell.tsx: TS delta = 0 (before: 20794, after: 20794)

## Checklist Results

✅ useActiveRoundsCount() calls GET /api/v1/rounds?status=in_progress (line 164 of worktree, line 164 of main repo)
✅ Hook returns count from json.total (number) or json.data.length (array) fallback
✅ Badge renders on /rounds sidebar item when count > 0 (sidebarBadges['/rounds'] > 0 condition in nav loop)
✅ Badge disappears when count is 0 (condition is > 0, returns null otherwise)

## Cycle Check

NO COLLISION — attempt 1, no prior attempts to compare against.

## Why This Attempt Succeeded

Straightforward stub replacement. The worktree has an older version of AppShell.tsx that needed the useQuery import added plus badge rendering wired in. The main repo version already had badge rendering infrastructure — only needed the stub replaced. Both versions now correctly call the API with 60s refetch interval.

## Notes for Next Attempt

N/A
