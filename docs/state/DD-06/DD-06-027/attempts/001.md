---
task_id: DD-06-027
attempt: 1
result: SUCCESS
completed_at: 2026-03-26T00:00:00Z
---

## Work Done

Replaced hardcoded `#ef4444` with `var(--io-alarm-critical)` and `#fff` with `var(--io-text-inverse)` in the AlertBell component badge span at frontend/src/shared/layout/AppShell.tsx line 358-359.

The sidebar badge spans at lines 1427 and 1440 already correctly used `var(--io-alarm-critical, #ef4444)` and were left unchanged per spec instructions.

## Verification

TypeScript check run — pre-existing errors in file are unrelated to this change (WebSocket onAlarm* property issues on other lines). No new errors introduced.
