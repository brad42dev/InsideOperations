---
attempt: 1
task_id: DD-23-007
started: 2026-03-22T01:00:00Z
closed: 2026-03-22T01:20:00Z
result: SUCCESS
---

## What Was Attempted

Replaced the `handleApply` function in `ExpressionBuilder.tsx` (which was calling `onApply` directly) with a full multi-step async OK flow:

1. Added imports: `useCallback` from React, `* as Dialog` from `@radix-ui/react-dialog`, and `expressionsApi` from `../../../api/expressions`.
2. Added state variables: `nameError`, `showConfirmDialog`, `showSlowWarning`, `slowWarningAvgMs`, `showErrorDialog`, `okFlowRunning`, `pendingAstRef`.
3. Added `buildAst()` helper to construct the `ExpressionAst`.
4. Replaced `handleApply` with `handleOkClick` which validates name, sets `pendingAstRef`, and opens the confirmation dialog.
5. Added `handleConfirmOk` (useCallback async) which runs the Worker benchmark and routes to error/slow/proceed dialogs.
6. Added `doSaveAndApply` (useCallback async) which calls `expressionsApi.create` (when `saveForFuture`) before `onApply`.
7. Updated the OK button to call `handleOkClick`, show "Working..." while `okFlowRunning`, and disable on `okDisabled`.
8. Added inline name error display below the name input.
9. Added three Radix Dialog modals: confirmation, error, slow warning — with Cancel / Save for Later / Accept & Apply buttons.

## Files Modified

- `frontend/src/shared/components/expression/ExpressionBuilder.tsx` — full OK flow implementation replacing direct `onApply` call with multi-step confirm/benchmark/save/apply flow

## Patch Fingerprint

fingerprint: 235f1222adab262cd0f2939bc8b66f008b21072e401f8420e8c23f32da5cf552
before_state: 08cc392077f725551f9288bd85ec4150a331b864f44da994e79b0964d64c6464
after_state: 34446972a5fcf419dda7a6b64a625824335a7132f314f2656b71678d1fa6a81e

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit
result: PASS
output: clean (no errors)

## Checklist Results

✅ When saveForFuture is true and name is blank, handleOkClick shows inline error and returns without proceeding
✅ When saveForFuture is true, confirmation dialog "Test this expression, save it, and apply it?" shown
✅ Performance test (Worker benchmark) runs as part of the OK flow in handleConfirmOk
✅ If test passes, expressionsApi.create called before onApply in doSaveAndApply
✅ If test fails (error/null result), shows "This expression produces an error. Please check your formula." with Cancel / Save for Later
✅ If test is slow (>1ms avg), shows slow warning dialog with Cancel / Save for Later / Accept & Apply
✅ When saveForFuture is false, confirmation is "Test this expression and apply it?" with Apply / Cancel

## Cycle Check

NO COLLISION — no prior fingerprint matched (first attempt, no prior fingerprints)

## Why This Attempt Succeeded

All checklist items verified by code inspection. TypeScript build passes clean. The implementation follows the spec exactly: name validation before confirmation, Worker benchmark in the confirm flow, three dialog states (confirm/error/slow), API save before onApply, loading state on OK button.

## Notes for Next Attempt

N/A
