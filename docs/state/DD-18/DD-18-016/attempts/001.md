---
attempt: 1
task_id: DD-18-016
started: 2026-03-26T00:00:00Z
closed: 2026-03-26T00:03:00Z
result: SUCCESS
---

## What Was Attempted

Inserted a `aggregation_types` bitmask check in the `get_point_rolling` handler in `services/archive-service/src/handlers/history.rs`. The check queries `points_metadata` for the `aggregation_types` value of the requested `point_id`, and returns a 400 `IoError::BadRequest` if bit 0 (allow avg) is not set. The check is placed after `parse_window_seconds` and before the source-table selection block.

## Files Modified

- `services/archive-service/src/handlers/history.rs`: Added 14-line aggregation_types bitmask check block after `parse_window_seconds` call and before `if window_secs < 300 {` source-table selection.

## Changed Files

before_state: 8e311c29cc75ccd4041efbf0f45e3b92690a8722
changed_files:
  - services/archive-service/src/handlers/history.rs

changed_functions: (for human review only — not used in automated cycle detection)
  - none (only added inline code to existing get_point_rolling function)

## Verification

command: cargo check -p archive-service
result: PASS
output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.28s

command: cargo test -p archive-service
result: PASS
output: test result: ok. 2 passed; 0 failed; 2 ignored; 0 measured; 0 filtered out

## Checklist Results

✅ `get_point_rolling` looks up `aggregation_types` for the requested `point_id` — query at line 805-811
✅ If bit 0 (`allow avg`) is not set, the handler returns a 400 `IoError::BadRequest` before executing any SQL — check at line 813-817
✅ The error message clearly states the point does not permit averaging — "This point does not permit averaging (aggregation_types bit 0 not set)"
✅ When averaging is permitted, the existing rolling window logic is unchanged — source-table selection block at line 819+ untouched

## Cycle Check

NO COLLISION — no prior attempts exist for this task.

## Why This Attempt Succeeded

The fix was straightforward: a single sqlx::query_scalar lookup followed by a bitmask check inserted at the correct location. Build check and unit tests passed cleanly.

## Notes for Next Attempt

N/A
