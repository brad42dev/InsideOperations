---
attempt: 1
task_id: DD-32-005
started: 2026-03-22T00:00:00Z
closed: 2026-03-22T00:05:00Z
result: SUCCESS
---

## What Was Attempted

Rewrote `frontend/src/shared/components/PointContextMenu.tsx` to match the spec exactly:

1. Renamed "Trend This Point" → "Trend Point", changed navigation from `/forensics?...` to `/console?trend={pointId}`, added `console:read` permission gate.
2. Fixed "Investigate Point" RBAC from `forensics:read` → `forensics:write`. Changed action from simple navigate to POST `forensicsApi.createInvestigation({ name, anchor_point_id })` then navigate to `/forensics/investigations/{id}`.
3. Fixed "Report on Point" navigation from `/reports?point=` to `/reports/new?point=`.
4. Added `onTrendPoint?` and `onInvestigatePoint?` optional props for callers that want to intercept actions.
5. Added internal `internalOpen` state (useState) and `longPressTimer` ref (useRef) to support programmatic open from long-press touch events.
6. Added `onTouchStart`/`onTouchEnd`/`onTouchMove` handlers to the wrapper span: 500ms timer on TouchStart opens the menu; TouchEnd and TouchMove cancel the timer.
7. Changed `DropdownMenu.Root` to always pass `open` (internal or controlled) and `onOpenChange` so the programmatic open works.

## Files Modified

- `frontend/src/shared/components/PointContextMenu.tsx` — all changes as described above

## Patch Fingerprint

fingerprint: 9090e93892cb7dcb665ba21a3580306fc13f14b9b73907eb2b8c32288f9f1601
before_state: 89551b331c06a979ed9ca1a34451309056064da96251b78c5f67338d5706980f
after_state: 61fee248109e83f3d68a7af3800664e7e59f44245d71b78df1f29550937022d2

## Verification

command: cd /home/io/io-dev/io/frontend && npx tsc --noEmit 2>&1 | tail -30
result: PASS
output: clean (no output)

## Checklist Results

✅ Menu has exactly: "Point Detail", "Trend Point", "Investigate Point", "Report on Point" (old incorrect items removed)
✅ "Trend Point" navigates to /console?trend={pointId} (line 115)
✅ "Investigate Point" POSTs to forensicsApi.createInvestigation with anchor_point_id then navigates to /forensics/investigations/{id}
✅ "Report on Point" navigates to /reports/new?point={pointId} (line 134)
✅ Permission gates use conditional rendering ({canConsole && ...}, {canForensicsWrite && ...}, {canReports && ...}) — items not rendered when permission absent
✅ Long-press 500ms: onTouchStart sets 500ms timer; onTouchEnd/onTouchMove cancel it; timer callback calls triggerOpen()

## Cycle Check

NO COLLISION — no prior fingerprint matched (this is attempt 1, no prior fingerprints).

## Why This Attempt Succeeded

All six checklist items verified. TypeScript build passes cleanly. The changes are focused and mechanical: menu item labels and actions corrected, RBAC tightened to spec permissions, mobile long-press added via timer pattern.

## Notes for Next Attempt

N/A
