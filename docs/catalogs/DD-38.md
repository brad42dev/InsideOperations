---
unit: DD-38
audited: 2026-03-21
relationship: DESIGN-DOC-ONLY
spec-files: [design-docs/38_FRONTEND_CONTRACTS.md]
result: ⚠️ Gaps found
tasks-generated: 7
---

## Summary

The route map implementation is largely complete with all 11 module root routes and most sub-routes present. Six permission mismatches exist between App.tsx and the spec, ranging from low-risk alias differences to substantive wrong-permission assignments. The `RouteConfig[]` registry (`src/shared/routes/registry.ts`) required by the spec does not exist — routes are inlined directly in App.tsx. The CSS token registry is complete and correct across all three themes (138 tokens verified). The sidebar groups, ordering, G-key shortcuts, and permission-filtered visibility are correctly implemented. One false-DONE gap: `/designer/symbols` redirects to `/designer` instead of rendering the `SymbolLibrary` component.

## Non-Negotiables

| # | Non-Negotiable | Status | Evidence |
|---|---------------|--------|----------|
| 1 | All ~80 routes from doc 38 route map present in App.tsx | ⚠️ Wrong | 6 permission mismatches; 1 route stub-redirected instead of rendered (see below) |
| 2 | All 138 CSS design tokens present in tokens.ts for all 3 themes | ✅ | tokens.ts:1–649 — all three token maps verified, 138 tokens each |
| 3 | Sidebar navigation rules: groups, ordering, active states | ✅ | AppShell.tsx:39–71 — correct 4 groups, correct ordering, permission filter at line 578–580 |
| 4 | Route aliases: `/login/callback` and `/settings/imports` present | ✅ | App.tsx:111 (`/login/callback`→`OidcCallback`), App.tsx:956–963 (`/settings/imports`→`ImportSettingsPage`) |
| 5 | `src/shared/routes/registry.ts` with `RouteConfig[]` drives both router and sidebar | ❌ | File does not exist — router uses inline `<Route>` elements; sidebar uses `NAV_GROUPS` array in AppShell.tsx:39 |
| 6 | PermissionGuard component: unauthenticated→/login, no permission→ForbiddenPage | ✅ | PermissionGuard.tsx:79–81 |
| 7 | PermissionGuard EULA check: `!user.eula_accepted` → `/eula` | ⚠️ Wrong | PermissionGuard.tsx:75–82 — no EULA check inside guard; uses `EulaGate` wrapper at App.tsx:120 instead |
| 8 | Default redirect after login = first visible module per role | ⚠️ Wrong | App.tsx:127 — hardcoded `Navigate to="/settings/users"` regardless of user permissions |
| 9 | G-key shortcuts disabled when module hidden from sidebar | ⚠️ Wrong | AppShell.tsx:287–298 — `G_KEY_MAP` has no permission check; all 11 keys are always active |

## False-DONE Patterns

| Pattern | Present? | Evidence |
|---------|----------|----------|
| Route exists but wrong permission guard | ⚠️ Found | 6 instances — see Findings Summary |
| `/designer/symbols` renders redirect instead of `SymbolLibrary` | ⚠️ Found | App.tsx:314 — `<Navigate to="/designer" replace />` |
| `src/shared/routes/registry.ts` stub file with empty array | ✅ Not present | File does not exist at all |

## Findings Summary

- [DD-38-001] `src/shared/routes/registry.ts` with `RouteConfig[]` does not exist — not found
- [DD-38-002] `/designer/symbols` route redirects to `/designer` instead of rendering `SymbolLibrary` component — App.tsx:314
- [DD-38-003] `/settings/roles` uses `system:manage_users` permission, spec requires `system:manage_roles` — App.tsx:820
- [DD-38-004] `/settings/email` uses `settings:write` permission, spec requires `email:configure` — App.tsx:941
- [DD-38-005] `/settings/imports/connections`, `/definitions`, `/history` use `settings:write`, spec requires `system:import_connections`, `system:import_definitions`, `system:import_history` — App.tsx:983–1005
- [DD-38-006] `/settings/recognition` uses `settings:write`, spec requires `settings:admin` — App.tsx:1009
- [DD-38-007] Default post-login redirect is hardcoded to `/settings/users`; spec requires first visible module in sidebar order — App.tsx:127
