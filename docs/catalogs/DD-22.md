---
unit: DD-22
audited: 2026-03-21
relationship: DESIGN-DOC-ONLY
spec-files: []
design-doc: design-docs/22_DEPLOYMENT_GUIDE.md
result: ⚠️ Gaps found
tasks-generated: 7
---

## Summary

The deployment infrastructure is substantially built — systemd units, nginx config, installer scripts, health endpoints, and YARA-X scanning are all present and largely correct. Seven gaps were found: the Alert Service uses `Restart=on-failure` instead of the spec-required `Restart=always`; the Import Service is missing `Nice=10`; 9 of 11 services are missing the sd_notify watchdog keepalive call; the nginx config has HSTS enabled with preload (spec says off by default); the env template is missing several required variables (`TILE_RETENTION_HOURS`, `ALERT_HEARTBEAT_URL/INTERVAL`, `OPC_BUFFER_*`, `EXPORT_*`, `BACKUP_*`, `AUTH_MFA_*`); no ACME cert renewal systemd timer or internal renew endpoint exists; and `io-ctl generate-master-key` referenced in the install step is not implemented.

## Non-Negotiables

| # | Non-Negotiable | Status | Evidence |
|---|---------------|--------|----------|
| 1 | All 11 services have systemd units with WatchdogSec=30 | ✅ | `systemd/io-*.service` — all 11 service units have `WatchdogSec=30s` |
| 2 | Alert Service uses `Restart=always`, RestartSec=3s | ⚠️ Wrong | `systemd/io-alert-service.service:L9` — `Restart=on-failure`, `RestartSec=5s` (spec: `Restart=always`, `RestartSec=3s`) |
| 3 | Import Service has `Nice=10` (QoS tier 3) | ❌ Missing | `systemd/io-import-service.service` — no `Nice=` directive found |
| 4 | All 11 services call sd_notify WATCHDOG=1 at runtime | ⚠️ Wrong | Only `api-gateway/src/main.rs:76` and `auth-service/src/main.rs:46` call `start_watchdog_keepalive()`; the other 9 services (`data-broker`, `opc-service`, `event-service`, `parser-service`, `archive-service`, `import-service`, `alert-service`, `email-service`, `recognition-service`) do not |
| 5 | nginx has HSTS off by default | ⚠️ Wrong | `nginx/io.nginx.conf:40` — `add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;` is unconditionally set (spec: disabled by default) |
| 6 | Health endpoints at `/healthz` per design doc table | ⚠️ Wrong | `crates/io-health/src/lib.rs:99-101` — endpoints are `/health/live`, `/health/ready`, `/health/startup`; design doc monitoring table lists `/healthz` for all services. (Note: upgrade section uses `/health/ready`, so the path difference may be a doc inconsistency — services do implement functioning health routes) |
| 7 | Health check response has `db` field per spec format | ⚠️ Wrong | `crates/io-health/src/lib.rs:166-174` — response uses `"checks"` dict; spec shows `"db":"connected"` as a top-level field. Structural mismatch with the documented wire format. |
| 8 | ACME cert renewal timer (`io-cert-renew.timer` + `.service`) exists | ❌ Missing | `systemd/` directory — no `io-cert-renew.timer` or `io-cert-renew.service` found |
| 9 | Internal renew endpoint exists for cert renewal service to call | ❌ Missing | `services/api-gateway/src/` — no `/api/internal/certs/renew` route found |
| 10 | `io-ctl generate-master-key` binary exists | ❌ Missing | `Cargo.toml` — no `io-ctl` workspace member; `services/` — no `ctl` directory |
| 11 | io.env.example contains all required env vars | ⚠️ Wrong | `config/io.env.example` — missing: `TILE_RETENTION_HOURS`, `ALERT_HEARTBEAT_URL`, `ALERT_HEARTBEAT_INTERVAL_SEC`, `OPC_BUFFER_MEMORY_SECONDS`, `OPC_BUFFER_DISK_PATH`, `OPC_BUFFER_DISK_MAX_MB`, `EXPORT_RETENTION_HOURS`, `EXPORT_MAX_WORKERS`, `EXPORT_PER_USER_LIMIT`, `EXPORT_MAX_FILE_SIZE_MB`, `BULK_UPDATE_MAX_UPLOAD_MB`, `BULK_UPDATE_MAX_ROWS`, `BACKUP_RETENTION_COUNT`, `BACKUP_SCHEDULE`, `AUTH_MFA_GRACE_PERIOD_HOURS`, `AUTH_MFA_MAX_FAILURES`, `AUTH_MFA_LOCKOUT_DURATION_SEC`, `IO_CERT_DIR`, `IO_ACME_WEBROOT`, `IO_CERT_RENEW_DAYS` |
| 12 | Deployment profiles (Standalone/Resilient/Enterprise HA) documented, nginx config handles Resilient multi-host DB | ✅ | Design doc §Deployment Profiles is implemented as documentation; installer handles Standalone (all services on one host). `nginx/io.nginx.conf` is present. |
| 13 | Package script (`installer/package.sh`) and deploy script (`installer/deploy.sh`) exist | ✅ | `installer/package.sh` and `installer/deploy.sh` both present and complete |
| 14 | Smoke test suite hits all 11 service health endpoints | ✅ | `installer/smoke-test.sh:71-75` — loops over all 11 service:port pairs and checks `/health/live` |
| 15 | YARA-X content scanning compiled into API Gateway | ✅ | `crates/io-scan/src/lib.rs` — `yara-x` feature-gated scan; `api-gateway/Cargo.toml` has `io-scan` dep; `Cargo.toml:31` has `yara-x` workspace dep |
| 16 | resvg tile generation in API Gateway | ✅ | `api-gateway/Cargo.toml` — `resvg.workspace = true`, `tiny-skia.workspace = true` |
| 17 | nginx tile location block present | ✅ | `nginx/io.nginx.conf:68-72` — `/tiles/` alias block present |
| 18 | Self-signed cert auto-generation on first startup | ❌ Missing | `services/api-gateway/src/` — no `rcgen` usage found; `api-gateway/Cargo.toml` — no `rcgen` dep. The `handlers/certificates.rs` handles upload/list/delete but not auto-generation. |

## False-DONE Patterns

| Pattern | Present? | Evidence |
|---------|----------|----------|
| Alert service with `Restart=on-failure` instead of `Restart=always` (safety-critical service should always restart) | ⚠️ Found | `systemd/io-alert-service.service:L9` |
| Health endpoints exist but watchdog notify not wired in most services | ⚠️ Found | 9 of 11 `main.rs` files — `start_watchdog_keepalive()` never called |
| nginx config with HSTS preload hardcoded on | ⚠️ Found | `nginx/io.nginx.conf:40` — HSTS on with `preload` flag |
| env template present but incomplete for all spec-required variables | ⚠️ Found | `config/io.env.example` — 20+ spec-required env vars absent |

## Findings Summary

- [DD-22-001] Alert Service restart policy wrong — should be `Restart=always`, `RestartSec=3s` — `systemd/io-alert-service.service:9-10`
- [DD-22-002] Import Service missing `Nice=10` for QoS tier 3 — `systemd/io-import-service.service`
- [DD-22-003] 9 of 11 services not wired to call sd_notify watchdog keepalive — `services/*/src/main.rs`
- [DD-22-004] nginx HSTS hardcoded on with preload — should be off by default per spec — `nginx/io.nginx.conf:40`
- [DD-22-005] io.env.example missing ~20 spec-required environment variables — `config/io.env.example`
- [DD-22-006] ACME certificate renewal timer and API endpoint not implemented — `systemd/`, `services/api-gateway/src/`
- [DD-22-007] `io-ctl generate-master-key` command not implemented — referenced in install step 7
