---
unit: DD-33
audited: 2026-03-21
relationship: DESIGN-DOC-ONLY
spec-files: [design-docs/33_TESTING_STRATEGY.md]
result: ❌ Major gaps
tasks-generated: 7
---

## Summary

Testing infrastructure is partially in place but substantially incomplete against the spec. The CI pipeline exists (commit + nightly) but is missing the PR-stage E2E run, the PR-stage frontend integration tests, the Prettier check, and the weekly workflow entirely. Nightly is missing performance benchmarks and the Goose load test job. Eight of eleven services have zero unit test blocks. No criterion benchmarks exist anywhere in the workspace. The eight E2E critical path files are stub-level — most execute only a URL navigation check. axe-core accessibility testing has no integration in E2E tests.

## Non-Negotiables

| # | Non-Negotiable | Status | Evidence |
|---|---------------|--------|----------|
| 1 | CI commit stage: Rust unit tests, frontend unit tests, clippy, rustfmt, ESLint, Prettier, tsc — all < 2 min | ⚠️ Wrong | `.github/workflows/ci.yml` — Prettier check absent; ESLint present via `pnpm lint` but Prettier not invoked |
| 2 | CI PR stage: integration tests (Rust + frontend), E2E critical paths (8 tests), accessibility checks | ⚠️ Wrong | ci.yml:73 — Rust integration tests present; frontend integration tests absent; E2E critical paths absent from PR trigger (only in nightly.yml:25); accessibility checks absent |
| 3 | CI nightly: full E2E suite, criterion benchmarks, Goose load tests, security tests | ⚠️ Wrong | nightly.yml — security audit ✅, E2E stub present but no criterion job and no Goose load test job |
| 4 | CI weekly: extended security tests, `cargo audit`, `npm audit`, license check | ⚠️ Wrong | No weekly workflow exists; only nightly.yml with a daily cron; no `.github/workflows/weekly.yml` or equivalent |
| 5 | Backend unit tests: all pure functions and core business logic covered; io-* crates fully covered | ⚠️ Wrong | 8 of 11 services have 0 `#[cfg(test)]` blocks (data-broker, opc-service, parser-service, archive-service, import-service, alert-service, email-service, recognition-service); io-db, io-opc, io-export, io-health, io-observability have 0 test blocks |
| 6 | Backend integration tests: each of 11 services has integration test suite in `tests/` | ❌ Missing | Only `services/api-gateway/tests/security.rs` exists (8 tests, all `#[ignore]`); no other service has a `tests/` directory |
| 7 | Performance benchmarks with criterion on hot paths (serialization, Rhai eval, Pearson/FFT, resvg tile, alarm batch) | ❌ Missing | No `benches/` directory found anywhere in workspace; `criterion` not in workspace Cargo.toml |
| 8 | Goose load test: 200 concurrent WS connections with active subscriptions, 10k point updates/sec | ⚠️ Wrong | `tests/load/src/main.rs` exists with 5 HTTP scenarios; no WebSocket load scenario; the 10k point/sec scenario is absent; load test not triggered in any CI workflow |
| 9 | Frontend unit tests: Vitest + Testing Library + happy-dom | ✅ | `vite.config.ts:30` — environment: 'happy-dom', setupFiles: './src/test/setup.ts'; 24 test files present in `src/test/` |
| 10 | Frontend integration tests: module-level workflow tests with msw | ⚠️ Wrong | msw installed (package.json:65, server.ts exists); but no module-level workflow integration tests found — only unit-level tests exist in `src/test/` |
| 11 | E2E: Playwright configured, 8 critical path tests present | ⚠️ Wrong | playwright.config.ts exists; 8 spec files in `e2e/` but content is stub-level (page-load checks only, not the actual critical path flows described in spec §E2E) |
| 12 | E2E: axe-core accessibility audits run on every page during E2E tests | ❌ Missing | `axe-core` not in package.json; no `@axe-core/playwright` or `@playwright/test` accessibility API calls in any e2e spec |
| 13 | Test database: testcontainers for Rust integration tests; Docker Compose for E2E | ⚠️ Wrong | CI uses a `services:` block for PostgreSQL in the integration-tests job (correct for CI); `testcontainers` crate not in workspace Cargo.toml so test isolation model is incomplete |
| 14 | Rust test libraries: criterion, wiremock, testcontainers, fake, assert-json-diff in workspace | ❌ Missing | None of these crates present in `Cargo.toml` workspace.dependencies; only `goose` in `tests/load/Cargo.toml` |
| 15 | Security tests: auth flows, RBAC per-permission, input validation, rate limiting, YARA-X file scanning | ⚠️ Wrong | `services/api-gateway/tests/security.rs` covers 8 cases (unauth 401, expired JWT, RBAC 403, rate limit, SQL injection, XSS, oversized payload, CORS); all marked `#[ignore]`; no TOTP/MFA tests, no YARA-X file scan tests, no per-permission RBAC sweep for all 118 permissions |
| 16 | No random data in assertions; deterministic fixture files or builders | ⚠️ Wrong | No fixture factory files found; `fake` crate not declared; no deterministic time-series generator |
| 17 | Nightly E2E: extended suite (full CRUD, import/export wizard, bulk update, multi-user, mobile viewport, multi-window) | ❌ Missing | nightly.yml E2E job runs the same `npx playwright test` command as local; no extended suite separation; no mobile viewport tests; no multi-user or multi-window tests |

## False-DONE Patterns

| Pattern | Present? | Evidence |
|---------|----------|----------|
| E2E tests that only check page navigation (not actual workflow) | ⚠️ Found | `e2e/06-forensics.spec.ts:9` — sole test navigates to `/forensics` and checks URL; `e2e/08-emergency-alert.spec.ts:9` — sole test navigates to `/alerts` and checks URL; `e2e/04-alarms.spec.ts:9` — sole test navigates to `/settings/alarm-definitions` |
| Criterion benchmarks replaced by comments or missing entirely | ⚠️ Found | No `benches/` directories anywhere in workspace |
| Load test exists but is not wired into CI | ⚠️ Found | `tests/load/src/main.rs` implements Goose scenarios; no CI job invokes `cargo run -p io-load-tests` |
| Security tests marked `#[ignore]` with no CI job to run them | ⚠️ Found | `services/api-gateway/tests/security.rs` — all 8 tests have `#[tokio::test] #[ignore]`; nightly.yml has no `-- --ignored` invocation |
| Weekly CI described in spec has no workflow file | ⚠️ Found | Only `ci.yml` and `nightly.yml` exist in `.github/workflows/`; no `weekly.yml` |

## Wave 0 Contract Gaps

Not applicable — DD-33 is a DESIGN-DOC-ONLY unit covering testing infrastructure, not a module with Wave 0 obligations.

## Findings Summary

- [DD-33-001] CI commit stage missing Prettier check — `.github/workflows/ci.yml`
- [DD-33-002] CI PR stage missing E2E critical-path run and frontend integration tests — `.github/workflows/ci.yml`
- [DD-33-003] CI nightly missing criterion benchmark job and Goose load test job; Goose load test not invoked — `.github/workflows/nightly.yml`
- [DD-33-004] Weekly CI workflow missing entirely (license scan, extended security, dependency audit) — `.github/workflows/`
- [DD-33-005] 8 of 11 services and 5 of 12 shared crates have zero unit tests — `services/*/src/*.rs`, `crates/*/src/*.rs`
- [DD-33-006] No service-level integration test suites (only api-gateway security.rs exists, all `#[ignore]`); test crate dependencies (criterion, wiremock, testcontainers, fake, assert-json-diff) not declared in workspace — `Cargo.toml`
- [DD-33-007] E2E spec files are stub-level (page-load only); axe-core accessibility not integrated; no extended nightly E2E suite — `frontend/e2e/`
