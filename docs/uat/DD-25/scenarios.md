# UAT Scenarios — DD-25

## Export System (Backend + some UI)
Scenario 1: [DD-25-001] Settings page renders without error — navigate to /settings → page loads, no error boundary
Scenario 2: [DD-25-004] Console renders without error — navigate to /console → page renders, no error boundary
Scenario 3: [DD-25-007] Login and navigation work — navigate to /console → full app shell visible, navigation items present
