# UAT Scenarios — DD-25

## Export Page
Scenario 1: [DD-25-008] Export/Bulk Update page renders — navigate to /settings (export section) or look for "Export" nav item → page renders without error boundary
Scenario 2: [DD-25-001] Export options page shows format choices — find export section → 6 format options visible (CSV, JSON, Excel, etc.)
Scenario 3: [DD-25-008] Bulk Update wizard accessible — find bulk update section → wizard UI shows at least Step 1 (not blank)
Scenario 4: [DD-25-004] Bulk Update wizard has Step 2 — start bulk update wizard → Step 2 (Validate & Map) step present in wizard progress
Scenario 5: [DD-25-006] Change Snapshots section accessible — find change snapshots → list of snapshots shown or empty state
Scenario 6: [DD-25-007] My Exports section renders — find "My Exports" or "Exports" list → user's export list renders without error
