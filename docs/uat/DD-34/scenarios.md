# UAT Scenarios — DD-34

## DCS Import Wizard Access

Scenario 1: [DD-34-007] Designer loads without error — navigate to /designer → page renders with no error boundary

## DCS Import Job History

Scenario 2: [DD-34-007] DCS import wizard accessible — locate and trigger DCS import wizard from Designer → import wizard UI opens (button/menu item visible)

Scenario 3: [DD-34-007] Import job history visible — navigate to /designer/import or open DCS import wizard → import job history section or list visible (or empty state "No import history" / "No imports yet")

Scenario 4: [DD-34-007] Empty state shown when no imports — navigate to import history area → empty state message visible (not a blank page or 404)

Scenario 5: [DD-34-007] Import wizard step 1 renders — open DCS import wizard → step 1 visible (file upload or platform selection step)
