# UAT Scenarios — DD-34

## DCS Import Settings
Scenario 1: [DD-34-004] DCS import settings page renders — navigate to /settings, find DCS or import section → page loads without error boundary
Scenario 2: [DD-34-006] Platform list shows correct platforms — in DCS import config → platform dropdown or list shows spec-defined platforms (Honeywell, Emerson, ABB, Siemens, etc.)
Scenario 3: [DD-34-004] DCS import job list visible — navigate to DCS import jobs section → job list renders or empty state shown (not 404 or error)
Scenario 4: [DD-34-004] Import job creation UI accessible — click "New Import" or similar → import creation wizard/form appears
