# UAT Scenarios — DD-34

## DCS Graphics Import
Scenario 1: [DD-34-007] Import job history visible — navigate to /designer/import → import job history list or empty state visible alongside wizard
Scenario 2: [DD-34-004] Import wizard renders — /designer/import → 6-step wizard renders without error
Scenario 3: [DD-34-007] Import history section shows status — if any past imports exist, they show status (completed/failed/in-progress); if none, "No import history" message shown
