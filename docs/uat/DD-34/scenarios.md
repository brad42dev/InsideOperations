# UAT Scenarios — DD-34

## DCS Import (Backend unit — limited browser testability)
Scenario 1: [DD-34-001] Settings page loads without error — navigate to /settings → page loads, no error boundary
Scenario 2: [DD-34-004] DCS import UI accessible — navigate to /settings → DCS import or graphics import section visible
Scenario 3: [DD-34-006] Platform list visible in import UI — navigate to DCS import section → platform selection (Emerson, ABB, Honeywell, etc.) visible
