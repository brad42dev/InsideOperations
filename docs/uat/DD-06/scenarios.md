# UAT Scenarios — DD-06

## App Shell
Scenario 1: [DD-06-012] Console renders without error — navigate to /console → page loads, full app shell visible, no error boundary
Scenario 2: [DD-06-012] No popup banner visible on normal load — navigate to /console → no popup-blocked banner visible (banner only appears when popups are blocked)
Scenario 3: [DD-06-012] App shell navigation complete — navigate to /console → sidebar shows all module links (Console, Process, Dashboards, Reports, Forensics, Log, Rounds, Alerts, Shifts, Settings, Designer)
