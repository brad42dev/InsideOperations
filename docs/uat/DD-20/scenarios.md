# UAT Scenarios — DD-20

## Rounds Mobile Viewport
Scenario 1: [DD-20-006] Rounds page renders at mobile viewport — navigate to /rounds at 375px width → page renders without overflow or broken layout
Scenario 2: [DD-20-003] App renders at mobile viewport — navigate to /console at 375px → no error boundary, content visible
Scenario 3: [DD-20-005] Rounds page accessible on mobile — navigate to /rounds at 375px → main content and navigation accessible (no elements unreachable)
Scenario 4: [DD-20-004] Round player shows offline-capable UI — navigate to /rounds, open a round → round player loads without requiring live connection indicator
