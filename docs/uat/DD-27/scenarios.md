# UAT Scenarios — DD-27

## Alert Engine (DD-27)

Note: Most DD-27 tasks are backend. Observable via /alerts UI.

Scenario 1: [DD-27-008] Alerts page renders — navigate to /alerts → page loads, no error boundary
Scenario 2: [DD-27-005] Alert templates visible — check alerts template section → template list renders
Scenario 3: [DD-27-006] Recipient rosters visible — check alerts recipient section → roster management UI visible
Scenario 4: [DD-27-002] Active alerts show in UI — navigate to /alerts → active alerts panel renders (even if empty)
