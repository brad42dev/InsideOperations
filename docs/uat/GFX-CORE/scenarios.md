# UAT Scenarios — GFX-CORE

## Page Renders

Scenario 1: [GFX-CORE-004] Designer page renders without error — navigate to /designer → page loads, SVG canvas visible, no error boundary text

Scenario 2: [GFX-CORE-004] Console page renders graphics without error — navigate to /console → page loads, no "Something went wrong" error boundary

## Pipe Rendering (GFX-CORE-004)

Scenario 3: [GFX-CORE-004] Shape palette visible in designer — navigate to /designer → a shape/element palette sidebar is visible containing pipe or shape tools

Scenario 4: [GFX-CORE-004] Pipe element can be added to canvas — in designer, drag or click a pipe/shape from the palette to the canvas → a pipe element appears on the SVG canvas

Scenario 5: [GFX-CORE-004] Pipe element renders without error after placement — after placing a pipe element → no error boundary, element visible in SVG canvas snapshot

## Analog Bar / Display Elements (GFX-CORE-005)

Scenario 6: [GFX-CORE-005] Designer loads display element palette — navigate to /designer → display elements (text readout, analog bar, or similar) are visible in the element palette or toolbar

Scenario 7: [GFX-CORE-005] Process module renders SVG graphics without error — navigate to /process → page loads, no error boundary, if graphic present SVG is visible
