# UAT Scenarios — DD-29

## Authentication

Scenario 1: [DD-29-010] Login page renders — navigate to /login → login form visible without error
Scenario 2: [DD-29-010] Session lock persists — trigger session lock → lock state persisted (re-navigating shows lock screen, not just client-side)
Scenario 3: [DD-29-011] PIN entry in lock screen — trigger lock screen → PIN entry option available alongside password
