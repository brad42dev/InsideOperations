# UAT Scenarios — DD-39

## iographic Format (DD-39) — /designer

Note: Most DD-39 tasks are backend export format fixes. Browser-testable via designer export flow.

Scenario 1: [DD-39-001] Designer page renders — navigate to /designer → page loads, no error boundary
Scenario 2: [DD-39-008] Export graphic from designer — open a graphic in designer → File → Export option available (correct route/method)
Scenario 3: [DD-39-002] Export dialog has tag binding option — check export flow → tag names used in bindings (not raw UUIDs shown to user)
Scenario 4: [DD-39-006] Shape sidecar data present — check shape panel in designer → shapes load with full metadata
