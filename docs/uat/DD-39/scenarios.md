# UAT Scenarios — DD-39

## .iographic Format

Scenario 1: [DD-39-009] Export option in Designer — navigate to /designer, open File menu → export as .iographic option present
Scenario 2: [DD-39-010] Custom shapes management UI — navigate to /designer → Symbol Library shows custom shapes section alongside built-in ISA-101 shapes
Scenario 3: [DD-39-011] Custom shapes backend accessible — navigate to custom shapes area → user shapes loaded (no 404 for /api/v1/shapes/user)
Scenario 4: [DD-39-003] iographic import wizard — find import option in Designer → wizard includes analyze step
Scenario 5: [DD-39-008] Export route works — use export feature → file exported without route/method errors
