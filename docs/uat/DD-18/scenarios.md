# UAT Scenarios — DD-18

## TimescaleDB / Archive (DD-18)

Note: DD-18 tasks are all backend/database changes. No direct browser UI for these. Testing via observable UI effects only.

Scenario 1: [DD-18-001] Historical trend loads — navigate to /forensics → historical trend data renders without error
Scenario 2: [DD-18-002] Historical data resolution options — open a trend/chart with time range → multiple resolution options available (including 15m, 1d if visible)
Scenario 3: [DD-18-005] Rolling average endpoint effect — check forensics trend chart → trend lines render without error
