# UAT Scenarios — DD-18

## TimescaleDB / Archive Service (Backend)
Scenario 1: [DD-18-001] Forensics page renders without error — navigate to /forensics → page loads, no error boundary
Scenario 2: [DD-18-003] Settings page renders without error — navigate to /settings → page loads, no error boundary  
Scenario 3: [DD-18-004] Console renders without error — navigate to /console → page loads, no error boundary
