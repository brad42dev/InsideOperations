# UAT Scenarios — MOD-CONSOLE

## Console Module Crash Fix
Scenario 1: [MOD-CONSOLE-015] Console loads without TypeError crash — navigate to /console → page loads successfully, no error boundary, no "Something went wrong"
Scenario 2: [MOD-CONSOLE-015] Workspace list renders or shows empty state — navigate to /console → either workspace rows or "No workspaces yet" empty state visible
Scenario 3: [MOD-CONSOLE-015] Console header and navigation visible — navigate to /console → "Console" heading visible, sidebar navigation present
