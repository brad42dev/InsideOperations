# UAT Scenarios — DD-37

## Wire Format / WebSocket (Backend unit — limited browser testability)
Scenario 1: [DD-37-002] Console receives live data updates — navigate to /console → data values update in real-time (or connection status shows connected)
Scenario 2: [DD-37-003] Presence/status indicators work — navigate to /console or /shifts → user presence indicators visible
Scenario 3: [DD-37-006] API key auth doesn't break normal login — navigate to /login, log in normally → login succeeds (API key auth doesn't interfere)
