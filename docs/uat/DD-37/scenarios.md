# UAT Scenarios — DD-37

## Wire Format / IPC Contracts (Backend)
Scenario 1: [DD-37-002] Console renders without error — navigate to /console → page loads, no error boundary, no JS type errors
Scenario 2: [DD-37-003] App shell loads — navigate to /console → service status panel visible in sidebar
Scenario 3: [DD-37-004] Console renders with navigation — navigate to /console → full app shell visible
Scenario 4: [DD-37-006] Login works with JWT auth — navigate to /login, sign in → redirects to /console successfully
