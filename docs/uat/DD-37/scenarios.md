# UAT Scenarios — DD-37

## IPC Contracts (Backend)
Scenario 1: [DD-37-001] App loads without type errors — navigate to /console → no TypeScript/type errors in console
Scenario 2: [DD-37-005] Paginated list loads — navigate to /settings → users list paginated correctly with correct field names
Scenario 3: [DD-37-006] API key auth works — log in with admin credentials → auth works (already tested)
