# UAT Scenarios — DD-28

## Email Service (Backend)
Scenario 1: [DD-28-002] Settings page renders without error — navigate to /settings → page loads, no error boundary
Scenario 2: [DD-28-005] Console renders without error — navigate to /console → page renders, no error boundary
Scenario 3: [DD-28-008] App shell loads — navigate to /console → navigation visible, no error boundary
