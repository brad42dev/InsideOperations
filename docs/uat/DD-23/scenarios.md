# UAT Scenarios — DD-23

## Expression Builder — field_ref Tile Type (DD-23-002)

Scenario 1: [DD-23-002] Expression builder page renders without error — navigate to /designer, open expression builder → no error boundary ("Something went wrong") visible
Scenario 2: [DD-23-002] Expression builder palette is visible — open expression builder → a tile palette/sidebar is visible with tile categories or tile items listed
Scenario 3: [DD-23-002] field_ref tile type appears in palette — open expression builder palette → a tile labeled "field_ref" or "Field Reference" is visible in the palette
Scenario 4: [DD-23-002] Expression builder opens from designer — navigate to /designer → designer canvas loads without error, primary UI elements visible
Scenario 5: [DD-23-002] Expression builder has tile workspace area — open expression builder → workspace area where tiles can be placed is visible
Scenario 6: [DD-23-002] Palette contains standard tile types — open expression builder → palette shows common tile types (e.g., literals, operators, or function tiles)
