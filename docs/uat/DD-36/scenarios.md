# UAT Scenarios — DD-36

## Observability / Metrics (Backend unit — limited browser testability)
Scenario 1: [DD-36-002] App loads without metrics errors — navigate to /console → page loads, no error boundary (metrics don't break frontend)
Scenario 2: [DD-36-003] Health status visible in app — navigate to app → connection/health indicators visible
Scenario 3: [DD-36-004] Settings page functional — navigate to /settings → settings page loads and renders correctly
