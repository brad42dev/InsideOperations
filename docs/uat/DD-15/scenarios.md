# UAT Scenarios — DD-15

## Settings Module

Scenario 1: [DD-15-001] Settings page renders — navigate to /settings → page loads, no error boundary
Scenario 2: [DD-15-002] MFA settings route — navigate to /settings/mfa → MFA settings page loads
Scenario 3: [DD-15-003] OPC source form has client cert dropdown — navigate to /settings/opc-sources → create/edit source form has client certificate dropdown
Scenario 4: [DD-15-004] Group management CRUD — navigate to /settings/groups or equivalent → create group button visible
Scenario 5: [DD-15-005] Role edit has idle timeout — open role edit dialog → idle timeout field visible
Scenario 6: [DD-15-006] Point Configuration page — navigate to /settings/points → point config page loads
Scenario 7: [DD-15-007] EULA has markdown editor — navigate to /settings/eula → markdown editor visible (split-pane)
Scenario 8: [DD-15-008] Export buttons in settings tables — check settings pages with tables → export buttons present
Scenario 9: [DD-15-009] Data category selection in source config — OPC source form → data category selector visible
