# UAT Scenarios — DD-14

## Rounds Print Feature
Scenario 1: [DD-14-008] Rounds page renders without error — navigate to /rounds → page loads, no error boundary
Scenario 2: [DD-14-008] Print/Export button visible in round view — open a round or template → print/export button visible in the UI
Scenario 3: [DD-14-008] Print dialog shows mode options — click print button → dialog appears with "Blank checklist" and "Current results" options
