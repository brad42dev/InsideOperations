# UAT Scenarios — OPC-BACKEND

## OPC Source Configuration
Scenario 1: [OPC-BACKEND-003] OPC settings page renders without error — navigate to /settings/opc-sources → page loads, no error boundary
Scenario 2: [OPC-BACKEND-003] OPC source list visible — navigate to /settings/opc-sources → OPC source list or empty state visible, not a stub
Scenario 3: [OPC-BACKEND-003] OPC source config form accessible — click add/edit on OPC source → form opens with configuration options
Scenario 4: [OPC-BACKEND-003] Deadband/filter settings in OPC config — open OPC source config → deadband or filter configuration option visible
Scenario 5: [OPC-BACKEND-003] OPC connection test available — navigate to /settings/opc-sources → test connection button visible
